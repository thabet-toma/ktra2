# FRONTEND_CHANGE — خطة تحديث واجهة K.T.R.A من نمط «الأصيل» إلى واجهة حديثة

> **وثيقة تنفيذية للموديل المنفّذ.** أُعدّت بتاريخ **2026-07** بصفة Staff Engineer / Tech Lead.
> **العقد الصارم: صفر فقدان ميزات.** كل خانة، كل زر، كل اختصار لوحة مفاتيح، كل تبويب، كل عمود جدول موجود اليوم يجب أن يبقى موجوداً وعاملاً بعد التحديث. التغيير **بصري فقط** (Skin/Chrome)، لا سلوكي.

---

## 0) الافتراضات المعلنة (Think Before Coding)

| # | الافتراض | الأساس |
|---|----------|--------|
| A1 | «تغيير الواجهة» = تحديث المظهر (ألوان/خطوط/حواف/ظلال/كروم الأدوات) مع بقاء نفس الهيكل الوظيفي والتخطيط العام لكل شاشة | نص الطلب: «بحذر شديد ما تفقد أي ميزة» |
| A2 | لا ترقية إصدارات كبرى (Vite 8 / TS 7) ضمن هذا المشروع — مشروع إعادة تجليد لا يُخلط بترقية toolchain | عزل المخاطر + No Feature Creep |
| A3 | نمط «الأصيل» القديم يبقى متاحاً كـ fallback عبر مفتاح تبديل حتى نهاية فترة التثبيت، ثم الافتراضي يصبح الحديث | شبكة أمان للمالك |
| A4 | الشاشات العامة (LandingPage / LoginPage / SignupPage) حديثة أصلاً — خارج النطاق | فُحصت: تستخدم Tailwind حديث |
| A5 | الباك إند لا يُمسّ إطلاقاً في هذا المشروع | النطاق فرونت إند فقط |

**إن تعارض أي افتراض مع رغبة المالك أثناء التنفيذ: توقف واسأل. لا تختر مساراً بصمت.**

---

## 1) البروتوكول الأول — الوعي الزمني والإصدارات

تاريخ النظام المُتحقق منه عبر shell: **2026-07**. أحدث الإصدارات المستقرة على npm بهذا التاريخ (تم جلبها فعلياً بـ `npm view`):

| الحزمة | المثبّت حالياً | الأحدث المستقر | القرار |
|--------|----------------|----------------|--------|
| react / react-dom | 19.2.x | 19.2.7 | **إبقاء** (patch تلقائي عبر ^) |
| vite | 6.2.x | 8.1.5 | **لا ترقية** — قفزة majors ×2، خارج النطاق |
| typescript | ~5.8.2 | 7.0.2 | **لا ترقية** — خارج النطاق |
| tailwindcss + @tailwindcss/vite | 4.3.0 | 4.3.3 | **إبقاء** ضمن ^4.3 (CSS-first `@theme` هو الأسلوب المعتمد وغير deprecated) |
| lucide-react | 0.555.0 | 1.25.0 | **لا ترقية** — v1 غيّرت أسماء أيقونات؛ مخاطرة بلا عائد بصري |
| @playwright/test | 1.60 | 1.61.1 | **إبقاء** |
| react-router-dom | 7.10 | 7.18.1 | **إبقاء** |

**قاعدة ملزمة للمنفذ:** لا تعديل على `package.json` إلا إذا فشل build — وعندها patch فقط. لا مكتبات UI جديدة (بلا shadcn / MUI / Radix): نظام التصميم موجود داخلياً وكافٍ.

---

## 2) جرد الواقع الحالي (نتيجة الفحص الفعلي للكود)

### 2.1 البنية الحاكمة — لماذا هذا المشروع جراحي وليس إعادة كتابة

الفحص كشف أن المظهر القديم **معزول أصلاً في طبقة واحدة**:

1. **`frontend_v2/styles/index.css`** (1391 سطراً):
   - أسطر 1–66: `@theme` — توكنات حديثة جاهزة (`--color-primary: #2563eb`، surfaces، spacing، radius، shadows) + وضع داكن `.dark` (سطر 69).
   - سطر ~99: «طبقة التوحيد» `:where()` بصفر specificity لكل inputs/selects/buttons.
   - **سطر ~345 حتى النهاية: كتلة `[data-skin="aseel"]`** — هنا يسكن كامل المظهر القديم: ~25 متغير `--aseel-*` (بيج `#e9e6d6`، Tahoma 12px، radius 2px) + **~140 كلاس `.aseel-*`** (titlebar/toolbar/grid/tabs/tree/totals/statusbar/picker/autocomplete/banner/badge...).
   - أقسام خاصة موجودة ويجب الحفاظ عليها: **Dark Aseel** (`N9-T3`, `data-theme="dark"`)، **توكنات N9-T1** البديلة عن ألوان Tailwind الخام، **Mobile RTL** (`N9-T4`)، **Print CSS** (`N9-T5`).

2. **`components/layout/AppLayout.tsx:87`** يضع `data-skin="aseel"` على جذر التطبيق المصادَق كله.

3. **63 ملفاً** يضعون `data-skin="aseel"` محلياً (منها ما هو لأجل الـ portals مثل `ProductCardModal` — الـ portal تحت `<body>` يفقد سمة الجدّ فيعيد تثبيتها).

4. **`components/aseel/`** — عدّة النظام: 17 مكوناً (`AseelDocumentShell`, `AseelDenseTable`, `AseelGrid`, `AseelAutocomplete`, `AseelTabs`, `AseelSidePanel`, `AseelIndexPicker`, `AseelDocumentView`, `AseelReportTable`, `AseelContextMenu`, `AseelDateInput`, `AseelFormSection`, `AseelStates`, `AseelCalculator*`, `AseelStatusBarItem`, `AseelKitStory`) + 4 hooks لوحة مفاتيح (`useAseelKeymap`, `useAseelFieldShortcuts`, `useAseelIndexKeymap`, `useRecordNavigation`).

**الاستنتاج المعماري:** بما أن كل شاشة (~85 صفحة lazy-loaded) ترسم عبر كلاسات `.aseel-*` الموحّدة والمظهر كله CSS-scoped، فإن التحديث = **استبدال جلد (Skin Swap) في طبقة CSS واحدة** + تنظيف كروم الهيكل. لا حاجة للمس منطق أي شاشة. هذا أقل كود يحل المشكلة (Simplicity First) وأدنى مخاطرة لفقدان ميزة.

### 2.2 عقد الميزات — قائمة "ممنوع فقدانها" (Feature Contract)

المنفذ ملزم بالحفاظ على كل ما يلي **بلا استثناء**:

- **78 شاشة** معرّفة في `types/common.ts` (union type `AppView`) — كلها يجب أن تفتح وتعمل.
- **عدّة الأصيل كاملة**: التنقل بين السجلات (الأول/السابق/التالي/الأخير)، شريط الأوامر، حزام الحقول العلوي، الشبكة، شريط التبويبات السفلي، رصيف المجاميع، شريط الحالة، الشجرة الجانبية (`aside`)، الحاسبة المنبثقة، منتقي الفهرس، القائمة السياقية.
- **اختصارات لوحة المفاتيح** جميعها: F11 (ثوابت المجموعة)، keymaps الحقول والشبكات في `useAseelKeymap`/`useAseelFieldShortcuts`/`useAseelIndexKeymap`، نقرة مفردة/مزدوجة على شجرة الأصناف، (i) في dropdown الأصناف.
- **كروم AppLayout**: CompanySwitcher، BranchSwitcher، المساعد الذكي، GlobalActionBar، GlobalSearch، WhatsNewButton، NotificationCenter (الجرس)، زر ثوابت المجموعة، AseelCalculatorButton، DensitySwitch (comfortable/compact)، PriceVisibilityToggle، ThemeToggle، زر تكرار الصفحة، تسجيل الخروج، Breadcrumb، اختصارات الوصول السريع (Zap)، Sidebar.
- **الأنظمة العرضية**: الوضع الداكن (كلا الطبقتين `.dark` و`data-theme="dark"` للأصيل)، RTL كامل، الكثافة، مقياس/عائلة الخط لكل شركة (AppearanceContext + خادم)، إخفاء الأسعار، الطباعة (`N9-T5` + صفحات `*PrintView`)، الجوال (`N9-T4`)، نظام Offline/PWA كاملاً (banners، مزامنة، تعارضات)، Toast/Confirm contexts، IdleTimeoutGuard، تنسيق الأرقام G1 (`utils/formatNumber`).
- **السلوكيات الدقيقة**: أعمدة الجداول القابلة للسحب/الفرز في `AseelDenseTable`، chips الأسعار في `AseelAutocomplete`، ألوان دائن/مدين (`aseel-text-credit/debit/paid/due`)، صفوف المجاميع، الترقيم (pagination).

### 2.3 خارج النطاق (No Feature Creep)

ممنوع على المنفذ: إضافة ميزات جديدة، تغيير نصوص عربية، تغيير ترتيب الحقول أو الأعمدة، تغيير سلوك أي زر، ترقية مكتبات، «تحسين» منطق، حذف كود «يبدو ميتاً»، إعادة تسمية كلاسات `.aseel-*` (أسماء الكلاسات API داخلي مستقر — يتغير **محتواها** لا اسمها).

---

## 3) القرار المعماري (Surgical Architecture)

### 3.1 الاستراتيجية: Skin Layer Swap عبر CSS Variables

```
الوضع الحالي:  [data-skin="aseel"] { متغيرات --aseel-* } + [data-skin="aseel"] .aseel-X { قواعد }
الوضع الهدف:   .aseel-X { قواعد هيكلية تقرأ من --aseel-* فقط }        ← مشتركة بين الجلدين
               [data-skin="aseel"]  { --aseel-*: القيم القديمة }        ← يبقى كما هو (fallback)
               [data-skin="modern"] { --aseel-*: قيم حديثة + دلتا هيكلية صغيرة }
```

- **مصدر الجلد واحد**: السمة توضع على `<html>` (`document.documentElement.dataset.skin`) فتصل تلقائياً حتى للـ portals تحت `<body>`، وتُزال السمات المحلية الـ63 المكررة.
- **مفتاح تبديل** في صفحة الإعدادات (localStorage بمفتاح tenant-scoped، افتراضي `aseel` أثناء التطوير، يُقلب إلى `modern` في M6). بلا تعديل باك إند (Simplicity First — حقل خادم اختياري لاحقاً، يُسجل في ORPHANS).
- **الوضع الداكن**: جلد `modern` يرث آلية `data-theme="dark"` الحالية نفسها بقيم داكنة حديثة.

### 3.2 هوية الجلد الحديث (مواصفة ملزمة للمنفذ)

| الجانب | القديم (aseel) | الحديث (modern) |
|--------|----------------|------------------|
| الخلفية العامة | بيج `#e9e6d6` | `--color-surface-2` (#f8fafc) |
| الألواح/الحقول | `#f2f0e4` / `#fcfcec` | أبيض `--color-surface` بحدود `--color-border` |
| الخط | Tahoma 12px | `IBM Plex Sans Arabic` 13px (`--font-size-base`) |
| الحواف | radius 2px | radius 6px (`--radius-md`) |
| الظلال | لا شيء | `--shadow-sm` للألواح، `--shadow-lg` للمنبثقات |
| اللون الرئيسي | أزرق باهت `#2f5f8f` | `--color-primary` #2563eb |
| شريط الأوامر | أزرار محفورة متلاصقة | أزرار ghost بأيقونة+نص، hover خفيف، فواصل شفافة |
| رأس الشبكة | `#d7d4be` | `--color-surface-3` بوزن 600 وحرف أوضح |
| صف محدد | `#cbd9e8` | `color-mix(primary 8%)` |
| التبويبات | مستطيلات محفورة | underline بلون primary |
| شريط الحالة | لوح رمادي | سطر رفيع `--color-text-muted` بحد علوي |

**قيد صارم:** الأبعاد الهندسية (ارتفاعات الصفوف، paddings الشبكة، عرض الأعمدة) تُحفظ ±2px حتى لا تنكسر الجداول الكثيفة أو تظهر scrollbars جديدة.

### 3.3 حارس التكافؤ الآلي (Feature-Parity Harness) — أهم بند في المشروع

قبل أي تغيير بصري يُبنى حارس آلي يجعل «عدم فقدان أي خانة» **قابلاً للقياس لا للانطباع**:

1. **إحصاء DOM لكل شاشة** (`e2e/feature-parity-census.spec.ts`): يسجل دخولاً، يزور كل view، وبعد استقرار الشبكة يحصي لكل شاشة: عدد `button` (بنصوصها)، `input/select/textarea` (بأسمائها/placeholders)، `[role=tab]`، رؤوس أعمدة الجداول (بنصوصها)، عناصر شريط الأدوات. يُكتب الناتج في `e2e/parity-baseline.json`.
2. **المقارنة**: نفس السكربت بوضع `PARITY_MODE=compare` يفشل إذا اختفى أي عنصر كان موجوداً في الأساس (الزيادة تحذير، النقصان **فشل فوري**).
3. **لقطات أساس** (`e2e/parity-shots/`) لكل شاشة قبل البدء — للمراجعة البشرية (لا pixel-diff صارم لأن الألوان ستتغير عمداً؛ البنية يحرسها الإحصاء).
4. **الحِزم الموجودة تبقى خضراء دائماً**: `npx playwright test` (20+ spec موجودة: offline، import journey، dense-table widths، titlebar alignment...) و`npm run test` (اختبارات utils) بعد **كل** مرحلة.

---

## 4) خطة المراحل (Milestones) — أهداف قابلة للتحقق

> **بروتوكول عام لكل تاسك:** اقرأ الملفات المذكورة كاملة قبل التعديل ← نفّذ ← `npm run build` ← اختبارات المرحلة ← أوقف وأبلغ إن ظهر أي انحراف. كل مرحلة commit مستقل قابل للرجوع.

---

### المرحلة M0 — التجميد والقياس (بلا أي تغيير بصري)

#### M0-T1: حارس التكافؤ

```prompt
أنت تعمل في مشروع K.T.R.A (فرونت إند: frontend_v2، React 19 + Vite 6 + Playwright).
أنشئ e2e/feature-parity-census.spec.ts:
- استعن بأنماط تسجيل الدخول والتهيئة الموجودة في e2e/import-journey-regressions.spec.ts وe2e/pw-offline-test-utils.ts (أعد استخدام أدواتها، لا تخترع مساراً جديداً).
- اجلب قائمة الشاشات من types/common.ts (union AppView) — رتّبها في مصفوفة ثابتة داخل السكربت مع تعليق يشير للمصدر.
- لكل شاشة: انتقل إليها (بنفس آلية التنقل التي تستخدمها الواجهة)، انتظر استقرار الشبكة، ثم احصب: أزرار (نصوصها بعد trim)، حقول إدخال (name/placeholder/aria-label)، تبويبات [role=tab] (نصوصها)، رؤوس جداول th/.aseel-dense-table headers (نصوصها)، عناصر .aseel-toolbtn.
- الوضع الافتراضي: اكتب النتيجة إلى e2e/parity-baseline.json (مفروزة ومستقرة deterministic).
- عند PARITY_MODE=compare: قارن مع الملف؛ أي عنصر موجود في الأساس ومفقود الآن ⇒ فشل الاختبار مع تقرير واضح (اسم الشاشة + العنصر). العناصر الجديدة تُطبع كتحذير فقط.
- الشاشات التي تتطلب معرّف سجل (partner-profile, product-profile, cash-box-details...) إن تعذر فتحها بدون بيانات: سجّلها في قسم "skipped" داخل الـ JSON بدل إسقاطها بصمت.
ممنوع: تعديل أي ملف خارج e2e/. الهدف القابل للتحقق: تشغيلان متتاليان يعطيان JSON متطابقاً، وcompare يمر على الكود الحالي.
```

#### M0-T2: لقطات الأساس

```prompt
أضف إلى نفس السكربت (أو ملف شقيق e2e/parity-shots.spec.ts) التقاط لقطة كاملة الصفحة لكل شاشة إلى e2e/parity-shots/baseline/<view>.png بوضع viewport ثابت 1440x900 وخط ثابت. أضف اللقطات إلى .gitignore إن تجاوز حجمها المعقول، مع README سطرين داخل e2e/ يشرح كيفية إعادة توليدها. لا تغييرات أخرى.
```

#### M0-T3: قنطرة الجلد (Skin Plumbing)

```prompt
في frontend_v2: أنشئ styles/skin.ts صغيراً (ملف واحد، بلا مجلد جديد):
- النوع: export type UiSkin = 'aseel' | 'modern'
- getSkin(): يقرأ localStorage (مفتاح 'ktra.uiSkin')، الافتراضي 'aseel'.
- setSkin(skin): يكتب localStorage + document.documentElement.dataset.skin + يبث حدث window 'ktra:skin'.
- applySkinOnBoot(): تُستدعى مرة من index.tsx وتضع السمة على <html>.
ثم:
1) استدعِ applySkinOnBoot() في index.tsx قبل الرندر.
2) في components/layout/AppLayout.tsx:87 احذف data-skin="aseel" (الجذر <html> صار المصدر).
3) امسح كل data-skin="aseel" المحلية في components/** (63 ملفاً — استخدم بحثاً شاملاً وتحقق أن grep النهائي يعيد صفراً خارج styles/skin.ts وAseelKitStory.tsx). AseelKitStory وحدها تبقى بسمة صريحة لكنها تقرأ getSkin().
4) في components/SettingsPage.tsx أضف حقل اختيار «مظهر الواجهة: كلاسيكي (الأصيل) / حديث» يستدعي setSkin ويطلب إعادة تحميل أو يطبق فوراً.
5) في styles/index.css: غيّر كل محددات [data-skin="aseel"] .aseel-X إلى :root[data-skin] .aseel-X أو ما يكافئها بحيث تعمل القواعد الهيكلية مع أي جلد، وأبقِ كتلة تعريف المتغيرات --aseel-* مربوطة بـ [data-skin="aseel"] فقط، وأضف مؤقتاً [data-skin="modern"] { /* placeholder: ينسخ قيم aseel حرفياً */ } كي لا يظهر أي فرق بصري في هذه المرحلة.
انتبه بدقة إلى: كتلة Dark Aseel (data-theme="dark") وكتلة Print وكتلة Mobile RTL — حدّث محدداتها بنفس المنطق دون تغيير أي قيمة.
الهدف القابل للتحقق: (أ) grep 'data-skin="aseel"' في components يعيد 0 (عدا الاستثناء المذكور)، (ب) الواجهة مطابقة 100% لما قبل التغيير على الجلدين (كلاهما aseel القيم)، (ج) PARITY_MODE=compare يمر، (د) npx playwright test وnpm run test وnpm run build خضراء.
```

**بوابة الخروج من M0:** baseline موجود + مقارنة خضراء + صفر فرق بصري.

---

### المرحلة M1 — طبقة التوكنات للجلد الحديث

#### M1-T1: تدقيق تسرب القيم الصلبة

```prompt
افحص كتلة الأصيل في styles/index.css (من سطر ~345 للنهاية): أي قاعدة .aseel-* تحتوي hex/rgb صلباً غير معرَّف كمتغير --aseel-* ⇒ استخرجه إلى متغير جديد بنفس القيمة داخل كتلة [data-skin="aseel"] (سمِّه دلالياً: --aseel-shadow-pop، --aseel-focus-ring...). لا يتغير أي بكسل. سجّل في تعليق أعلى الكتلة عدد المتغيرات النهائي. الهدف: القواعد الهيكلية صارت 100% مقادة بالمتغيرات؛ لقطات الشاشة قبل/بعد متطابقة بصرياً؛ كل الاختبارات خضراء.
```

#### M1-T2: توكنات الجلد الحديث (فاتح + داكن)

```prompt
استبدل placeholder كتلة [data-skin="modern"] بقيم فعلية وفق مواصفة الهوية في FRONTEND_CHANGE.md §3.2 — عرِّف كل متغيرات --aseel-* (نفس الأسماء، قيم حديثة) بحيث تُشتق من توكنات @theme الموجودة (--color-*, --radius-*, --shadow-*) عبر var() لا نسخ hex، كي يظل مصدر الحقيقة واحداً. أضف كتلة الوضع الداكن: [data-skin="modern"][data-theme="dark"] (أو التركيب المكافئ للبنية الحالية في قسم N9-T3) بقيم داكنة متسقة مع .dark الحالية.
القيد الهندسي: لا تغيّر في هذه التاسك أي خاصية layout (padding/height/width/display) — ألوان وخطوط وحواف وظلال فقط.
الهدف القابل للتحقق: التبديل من الإعدادات بين الجلدين يعمل حياً، جلد aseel لم يتغير بكسلاً، PARITY compare أخضر، الجداول الكثيفة بلا scrollbars جديدة (قارن e2e/aseel-dense-table-widths.spec.ts — يجب أن يبقى أخضر).
```

**بوابة M1:** الجلد الحديث يعرض كل الشاشات بألوان/خط حديثين وبنفس الهندسة، والقديم سليم.

#### سجل تنفيذ M1 (مُنجز — اقرأه قبل M2)

نُفّذت M1-T1 وM1-T2. اكتشافان بنيويان يغيّران افتراضات الخطة، **ملزمان لكل المراحل التالية**:

1. **الطباعة ليست شأن الجلد.** `contexts/AppearanceContext.tsx:87` يكتب `--aseel-font` و`--aseel-fs` و`--aseel-fs-sm` (+`--aseel-fs-base`/`--aseel-fs-title`) **أنماطاً inline على `<html>`** مضروبة في مقياس خط الشركة، والـ inline يتفوق على أي قاعدة CSS. لذلك بند «الخط 13px» في §3.2 **غير قابل للتنفيذ من طبقة الجلد** — وتنفيذه يتطلب تعديل منطق AppearanceContext وهو خارج نطاق مشروع بصري. القرار: توكنات الخط في الجلد الحديث صارت احتياطي ما-قبل-التركيب فقط (12px/11px = ما يكتبه المزوّد عند مقياس 1)، وتكبير الخط يبقى ميزة إعدادات المظهر. **ممنوع على M2–M5 محاولة فرض حجم خط من CSS الجلد — ستكون قاعدة ميتة.**
2. **`data-theme` لا يُضبط في أي مكان بالتطبيق.** مبدّل السمة (`ThemeContext.tsx:18`) يضيف كلاس `.dark` على `<html>` فقط. أي أن كتلة «N9-T3 Dark Aseel» `[data-skin="aseel"][data-theme="dark"]` **خاملة منذ كتابتها** — جلد الأصيل لا يستجيب للوضع الداكن أصلاً (ليست ردة فعل لهذا المشروع). تُركت كما هي (الأصيل مجمّد حتى M6) مع تعليق تحذيري. الجلد الحديث حُلّ عنده جذرياً: توكناته مشتقة عبر `var(--color-*)` التي ينقلبها `.dark` تلقائياً + كتلة `.dark[data-skin="modern"]` للشارات الدلالية ⇒ **الوضع الداكن يعمل فعلياً على الحديث** (مُتحقَّق منه بقياس القيم المحسوبة). في M5: القرار المطلوب من المالك هو مصير كتلة الأصيل الخاملة (ربطها بـ`.dark` أم حذفها).

ما تغيّر فعلياً في `styles/index.css`:
- استُخرجت كل قيمة صلبة من قواعد `.aseel-*` إلى توكنات (**60 توكناً** لكل جلد). لم يبقَ أي hex/rgba في قواعد الجلد عدا `@media print` (سوداء على أبيض عمداً في الجلدين — موثّق بتعليق، **لا تحوّلها لمتغيرات**).
- كتلة `[data-skin="modern"]` صارت قيماً حقيقية مشتقة عبر `var()` من `@theme` (`--color-*`/`--radius-*`/`--shadow-*`) لا نسخ hex.
- حُذف placeholder `[data-skin="modern"][data-theme="dark"]` (محدد لا يُطابَق أبداً).
- تعليق `.aseel-autocomplete-pop` القديم («البوابة خارج نطاق data-skin فالألوان مضمّنة») صار **غير صحيح** بعد M0-T3 (السمة على `<html>` والبوابات ترث) — صُحّح وحُوّلت قيمه لتوكنات.

بوابات مُثبتة: `npm run build` نظيف بلا تحذيرات · `npm run test` ‏42/42 · حزمة Playwright كاملة ‏53 passed / 3 skipped (مطابق لخط أساس M0) · `PARITY_MODE=compare` أخضر على الـ69 شاشة · لقطات مقارنة للجلدين على (فواتير المبيعات/الأصناف/قيد اليومية) أثبتت تطابق التخطيط وتحديث المظهر.

ملاحظة لـ M4: شريط «معلومات المرجع» في شاشة قيد اليومية يبقى كريمي اللون في **الجلدين** — لون Tailwind خام داخل المكوّن لا يمرّ بالتوكنات. هذا نموذج لما يجب أن يكنسه M4-T1.

---

### المرحلة M2 — كروم مكوّنات الأصيل (دلتا هيكلية للجلد الحديث فقط)

كل تاسك هنا CSS فقط داخل كتل `[data-skin="modern"]`، أسماء الكلاسات لا تُمسّ، وجلد aseel لا يتأثر.

#### M2-T1: شريط الأوامر وشريط العنوان

```prompt
داخل [data-skin="modern"] فقط في styles/index.css: حدّث .aseel-titlebar (خلفية surface، حد سفلي، العنوان chip يصبح عنوان صفحة بوزن 700 وحجم --font-size-xl، وحالة المستند .aseel-title-state تصبح badge ملوّنة بدل نص أحمر عارٍ)، و.aseel-toolbar/.aseel-toolbtn (أزرار ghost: خلفية شفافة، hover surface-3، أيقونة 16px مع نص، disabled بشفافية 0.45، --danger بلون danger، .aseel-toolsep حد شفاف بعرض 1px ولون border). حافظ على ارتفاع الشريط الحالي ±4px وعلى ترتيب أزرار التنقل بين السجلات (الأول/السابق/التالي/الأخير) كما هو.
تحقق: افتح فاتورة مبيعات وفاتورة شراء وصفقة على الجلد الحديث — كل الأزرار ظاهرة وقابلة للنقر، ثم PARITY compare + playwright suite.
```

#### M2-T2: حزام الحقول والنماذج والشبكات

```prompt
داخل [data-skin="modern"] فقط: .aseel-headband (خلفية surface بحد وradius-md بدل اللوح البيج)، .aseel-field/.aseel-field-label/.aseel-input (حقول بيضاء بحدود border وfocus ring من طبقة التوحيد، الـlabel بلون text-muted وزن 500)، .aseel-form-section (عنوان قسم بحد سفلي خفيف)، .aseel-grid/.aseel-dense-table (رأس surface-3 وزن 600، صف بديل surface-2، صف محدد color-mix primary 8%، خطوط شبكة border، خلايا الأرقام .aseel-num تبقى tabular-nums بنفس المحاذاة)، .aseel-total-row/.aseel-totals (رصيف مجاميع بلوح أبيض وحد، الصف الكلي --grand بوزن 700 وخلفية primary 6%). ممنوع تغيير ارتفاع الصفوف أو padding الخلايا بأكثر من 2px.
تحقق: e2e/aseel-dense-table-widths.spec.ts أخضر + سحب عرض الأعمدة يعمل + فرز الأعمدة يعمل + PARITY compare.
```

#### M2-T3: التبويبات، الشجرة، المنبثقات، الحالة

```prompt
داخل [data-skin="modern"] فقط: .aseel-tabs/.aseel-tab (تبويبات underline: شفافة، النشطة بحد سفلي 2px primary ونص primary)، .aseel-tree-* (لوح الشجرة أبيض بحد، عقدة hover بـ surface-3، المحددة primary 8%، الـ twisty بأيقونة حديثة عبر نفس الآلية الحالية)، .aseel-picker/.aseel-picker-mask/.aseel-autocomplete-pop/.aseel-ctxmenu/.aseel-date-cal (radius-lg + shadow-lg + حد border، mask بـ rgb(0 0 0/0.35))، .aseel-statusbar/.aseel-status-item (سطر رفيع بحد علوي، نص xs بلون muted، ألوان credit/debit/paid/due تبقى دلالية من توكنات success/danger/warning)، .aseel-banner--ok/warn/err و.aseel-badge-* و.aseel-price-badge-* (درجات حديثة فاتحة بنص داكن، وفي الداكن العكس — طابق نمط .badge-* الموجود أعلى الملف).
تحقق: منتقي الفهرس يفتح ويُغلق، الحاسبة تعمل، autocomplete الأصناف يعرض chips الأسعار، القائمة السياقية تظهر، ثم PARITY compare + كل الاختبارات.
```

**بوابة M2:** كل شاشات المستندات (فواتير/صفقات/قيود/جرد) تبدو حديثة كاملة على جلد modern، والقديم سليم حرفياً.

#### سجل تنفيذ M2 (مُنجز)

نُفّذت T1+T2+T3 ككتلة واحدة `M2 — كروم الجلد الحديث` في `styles/index.css`، محددها `:root[data-skin="modern"]`.

**قاعدة موضع حرجة (لا تكسرها):** الكتلة مُدرَجة **بعد** القواعد الهيكلية و**قبل** كتل `@media` (جوال N9-T4/767 وطباعة N9-T5). السبب أن المحدد `[data-skin="modern"] .aseel-X` وزنه (0,2,0) — **أقل** من الهيكلي `:root[data-skin] .aseel-X` (0,3,0) فيخسر؛ لذا استُخدم `:root[data-skin="modern"]` (0,3,0) ويفوز بالترتيب لا بالوزن. نقل الكتلة للأسفل يُبطل قواعد الجوال والطباعة، ورفعها للأعلى يُبطل الجلد الحديث كله.

ما تغيّر بصرياً (الجلد الحديث فقط): العنوان صار عنوان صفحة بدل chip محفور وحالة المستند شارة pill؛ أزرار الأوامر ghost بأيقونة 16px وhover رمادي فاتح؛ حزام الحقول وأقسام النموذج بطاقات بيضاء بحد وظل خفيف؛ رأس الشبكة رمادي فاتح بوزن 600 وخطوط ناعمة وحلقة تركيز زرقاء؛ التبويبات صارت underline بدل المستطيلات؛ الشجرة والمنبثقات radius-lg بظل واضح؛ شريط الحالة سطر رفيع فاتح بدل اللوح الداكن `#1f2937`. **الهندسة محفوظة**: لا تغيير في ارتفاعات صفوف الشبكة ولا عرض الأعمدة؛ فروق الحشوة في الأشرطة ضمن ±4px.

**اكتشاف ثالث — الوضع الداكن لا يُحفظ إطلاقاً:** `contexts/ThemeContext.tsx:12` يبدأ دائماً `useState<Theme>('light')` بلا قراءة أو كتابة في localStorage، والكلاس `.dark` يُضاف عند التبديل فقط. النتيجة: المستخدم يفقد الوضع الداكن مع كل تحديث للصفحة (سلوك قائم قبل هذا المشروع). لم يُلمس — إصلاحه تغيير منطق TypeScript خارج نطاق مشروع بصري. **بند قرار للمالك في M5** مع مصير كتلة الأصيل الداكنة الخاملة.

بوابات مُثبتة: البناء نظيف · `npm run test` ‏42/42 · حزمة Playwright ‏53 passed / 3 skipped · `PARITY_MODE=compare` أخضر · لقطات فاتح+داكن على (قيد اليومية/فواتير المبيعات/الأصناف/الإعدادات) أثبتت تطابق التخطيط.

**ما رصدته اللقطات لـ M4 (ألوان خام داخل المكوّنات لا تمرّ بالتوكنات — تظهر فاتحة شاذة في الوضع الداكن):** شريط «معلومات المرجع» في `AccountingJournalEntryPage`، وبانر `NoSqlMigrationBanner` أعلى كل الشاشات.

---

### المرحلة M3 — هيكل التطبيق (Shell)

#### M3-T1: رأس التطبيق والشريط الجانبي

```prompt
حدّث components/layout/AppLayout.tsx وcomponents/Sidebar.tsx بصرياً فقط (className فقط — ممنوع لمس أي prop أو handler أو ترتيب عناصر):
- استبدل كل ألوان Tailwind الخام (text-gray-500، bg-gray-100، dark:bg-gray-800...) بتوكنات var(--color-*) أو كلاسات N9-T1 الموجودة في index.css.
- رأس التطبيق: خلفية surface بحد سفلي border وارتفاع مضبوط، مجموعات الأزرار بمسافات متسقة gap-2، الأيقونات 16px.
- Sidebar: خلفية surface، البند النشط primary 8% بنص primary وحد جانبي 2px، الأقسام بعناوين xs muted. حافظ على سلوك الطي الحالي كما هو.
- Breadcrumb وGlobalActionBar وGlobalSearch وDensitySwitch وNotificationCenter: توكنات فقط، صفر تغيير سلوكي.
كل عنصر من عناصر الرأس الـ15 (CompanySwitcher, BranchSwitcher, المساعد الذكي, GlobalActionBar, معلومات المستخدم/التاريخ/السنة المالية, GlobalSearch, WhatsNewButton, NotificationCenter, ثوابت المجموعة, الحاسبة, DensitySwitch, PriceVisibilityToggle, ThemeToggle, تكرار الصفحة, تسجيل الخروج) يجب أن يبقى ظاهراً وعاملاً — عدّها قبل وبعد.
تحقق: e2e/titlebar-alignment.spec.ts أخضر + PARITY compare + لقطة مقارنة يدوية.
```

**بوابة M3:** الهيكل كله حديث ومتسق مع كروم M2 في الجلدين (الهيكل مشترك — تأكد أنه لا يزال مقروءاً فوق جلد aseel أيضاً).

#### سجل تنفيذ M3 (مُنجز)

**قرار نطاق مهم:** الهيكل (رأس التطبيق + الشريط الجانبي) **مشترك بين الجلدين** — لا يحمل كلاسات `.aseel-*` بل Tailwind مباشرة، فلا يمكن قصر تغييره على الجلد الحديث دون منطق شرطي في TSX. وهذا يصطدم بقاعدة «جلد الأصيل مقدّس». الحل المعتمد: **استبدال توكنات فقط** (`gray-*`/`blue-*`/`red-*` ← `var(--color-*)`) بلا تغيير لغة التصميم (البند النشط يبقى primary صلباً، لا تحوّل لـ tint بحد جانبي كما اقترحت §M3-T1 أصلاً). الفرق البصري على الأصيل ضئيل جداً (`gray-200`≈`--color-border`، `gray-500`≈`--color-text-muted`) والمكسب حقيقي: مصدر لون واحد + إصلاح الوضع الداكن.

الملفات المعدَّلة (className فقط — صفر تغيير في prop أو handler أو ترتيب عناصر): `AppLayout.tsx`، `Sidebar.tsx`، `Breadcrumb.tsx`، `CompanySwitcher.tsx`، `BranchSwitcher.tsx`، `WhatsNewButton.tsx`، `NotificationCenter.tsx`.

**عيوب وضع داكن حقيقية أُصلحت بالمناسبة** (كانت ألواناً صلبة بلا مقابل `dark:`): لوحة الإشعارات كانت `bg-white` ⇒ صندوق أبيض ساطع فوق واجهة داكنة؛ كتلة ملف المستخدم أسفل الشريط الجانبي؛ الشريط العلوي للجوال؛ زر طيّ الشريط الجانبي.

تحقق: `grep` للأنماط الخام في ملفات الهيكل السبعة يعيد **0** · عناصر الرأس الـ15 موجودة كلها (عدّ آلي) · `titlebar-alignment.spec.ts` أخضر · `npm run test` ‏42/42 · Playwright ‏53/3 · `PARITY_MODE=compare` أخضر · لقطات (حديث فاتح/حديث داكن/أصيل) مع لوحة الإشعارات مفتوحة أثبتت السلامة على الجلدين.

⚠ ملاحظة لـ M4: الألوان الخام في **الشاشات** لم تُمسّ بعد — M3 اقتصر على ملفات الهيكل السبعة.

---

### المرحلة M4 — كنس الشاشات (Token Sweep)

#### M4-T1: مسح الألوان الخام

```prompt
نفّذ مسحاً منهجياً على components/** بحثاً عن ألوان Tailwind خام (bg-gray-*, text-gray-*, bg-red-50, border-slate-*, ...) خارج الشاشات العامة (LandingPage/LoginPage/SignupPage/PublicGallery/AboutUs/Contact/TermsAndConditions — اتركها). استبدلها بتوكنات var(--color-*) أو كلاسات N9-T1، ملفاً ملفاً، بدفعات لا تتجاوز 10 ملفات لكل commit، مع npm run build بعد كل دفعة. لا تغيّر أي شيء غير قيم الألوان/الظلال/الحواف. أي حالة غامضة (لون له معنى دلالي غير واضح) ⇒ اتركها وسجلها في قائمة بنهاية التنفيذ.
الهدف: grep للأنماط الخام في الشاشات الداخلية يقارب الصفر، وكل الاختبارات خضراء بعد كل دفعة.
```

#### M4-T2: المودالات والتوستات

```prompt
وحّد مظهر المنبثقات على الجلد الحديث: ProductCardModal، ItemQuickCreateModal، CustomerQuickAddModal، مودالات المالية (finance/modals)، SupplierModal، ConfirmDialog/ConfirmContext، ToastContext — radius-lg، shadow-lg، رأس بعنوان وزن 600 وزر إغلاق، أزرار أسفل يمين (primary/ghost). className فقط أو عبر كتلة [data-skin="modern"] إن كانت المودال تستخدم كلاسات aseel. تذكر أن هذه المودالات portals — تأكد أنها ترث الجلد من <html> (اختبر على الجلدين).
تحقق: افتح كل مودال معدّل يدوياً على الجلدين + PARITY compare.
```

**بوابة M4:** لا بقايا رمادية/قديمة داخل الشاشات المصادَق عليها.

#### سجل تنفيذ M4 (مُنجز)

**القياس أولاً:** المسح الأولي أظهر 4562 موضع لون خام في 141 ملفاً — لكن تفكيكها غيّر الخطة: **1561 محايداً** (رماديات + `bg-white`) + **1132 متغير `dark:`** محايد + **1766 دلالياً** (أزرق/أحمر/أخضر/كهرماني للشارات والحالات). الدلالي **لم يُمسّ عمداً**: ألوانه تحمل معنى (نجاح/خطأ/تحذير) وتحويله لتوكنات محايدة يمحو المعنى. المستهدف كان المحايد وحده — وهو أيضاً سبب أعطال الوضع الداكن.

**التنفيذ آلي بحارسَين** (لا يدوي — 2700 موضع)، سكربت بخريطة مراجَعة طُبّق على 7 دفعات مع بناء بعد كل دفعة:
- **حارس العنصر الداكن المقصود:** `bg-gray-800` بلا بادئة `dark:` = عنصر داكن بالتصميم (رأس مستند، تلميح). تحويله يجعله أبيض بنص أبيض ⇒ غير مرئي. لا يُمسّ. (تحقق: `AseelDocumentView` احتفظ بـ`bg-slate-800 text-white`.)
- **حارس نظير الوضع الفاتح:** `dark:X` لا يُحذف إلا إذا كانت نفس الخاصية (وبنفس بادئة الحالة) قد حُوّلت لتوكن في السلسلة ذاتها — لأن التوكن يقلب وحده. `dark:bg-gray-800` وحيداً بلا نظير فاتح **يبقى** (حذفه يفقد الخلفية الداكنة).
- تمريرة ثانية للسلاسل خارج `className=` المباشر (خرائط الحالة، الدوال المُرجِعة لكلاسات، أطراف الشرط داخل `${...}`) بشرط أن **كل** رموز السلسلة تشبه كلاس Tailwind وفيها كلاس من الخريطة — فلا تُمسّ النصوص العربية ولا المسارات (`/api/...`) ولا `application/json`.

**النتيجة:** المحايد 1561 ← **157**، متغيرات `dark:` المحايدة 1132 ← **89**. 157 ملفاً معدَّلاً. المتبقي مقصود: عناصر داكنة بالتصميم، و`dark:bg-gray-700` على حقول إدخال تتولاها أصلاً طبقة التوحيد `:where()` (تعمل، وإن كانت درجتها لا تطابق التوكن تماماً).

**M4-T2 (المودالات):** ProductCardModal وui/Field وui/EmptyState كانت مُوكَّنة أصلاً؛ كُنست ItemQuickCreateModal وCustomerQuickAddModal وConfirmDialog وSupplierModal/SupplierViewModal ومودالات المالية الأربعة وCollapsibleSection.

**أعطال وضع داكن حقيقية زالت بالكنس** (كانت خلفيات/نصوص فاتحة بلا مقابل `dark:`): شاشة إدارة المهام كانت تبقى بيضاء بالكامل في الوضع الداكن؛ بانر `NoSqlMigrationBanner` أعلى كل الشاشات؛ أقسام نماذج الصفقات والفواتير؛ شاشات الصناديق والموردين.

بوابات مُثبتة: `tsc --noEmit` نظيف · البناء نظيف بعد كل دفعة · `npm run test` ‏42/42 · Playwright ‏53/3 · `PARITY_MODE=compare` أخضر · لقطات فاتح+داكن على (صفقات/فواتير شراء/صناديق/موردين/مهام).

⚠ متبقٍّ لمن يكمل: **1766 موضعاً دلالياً** لم تُمسّ. إن أُريد توحيدها لاحقاً فالمسار الصحيح ربطها بكلاسات `.badge-*` الموجودة في `index.css` (لها مقابل داكن جاهز)، لا تحويلها لتوكنات محايدة.

---

### المرحلة M5 — الأنماط العرضية (Cross-cutting)

```prompt
تحقّق وأصلح على الجلد الحديث حصراً (بدون كسر القديم):
1) الوضع الداكن: بدّل الثيم على 10 شاشات ممثلة (فاتورة مبيعات، فاتورة شراء، صفقة، قيد، ميزان مراجعة، جرد، شجرة حسابات، كرت صنف، كشف صندوق، الإعدادات) — لا نص غير مقروء ولا خلفية فاتحة شاذة. أصلح داخل كتلة الداكن للجلد الحديث فقط.
2) الطباعة: اطبع (معاينة) فاتورة مبيعات وفاتورة شراء وسند — كتلة Print CSS (N9-T5) يجب أن تعمل كما كانت؛ صفحات *PrintView لا تتأثر بالجلد.
3) الجوال RTL: عرض 375px على فاتورة وصفقة وقائمة أصناف — قواعد N9-T4 (حزام عمود واحد، toolbar صفّان) تعمل على الجلد الحديث.
4) الكثافة: وضعا comfortable/compact يعملان على الجلدين.
5) مقياس/عائلة الخط لكل شركة (AppearanceContext): غيّر المقياس وتأكد أن الجلد الحديث يحترمه (لا تكسر آلية font_scale).
أي إصلاح = CSS داخل كتل الجلد الحديث أو توكنات، لا منطق. الهدف: قائمة الفحوص الخمس موثقة نجاحاً في رسالة الإنجاز + الاختبارات كلها خضراء.
```

#### سجل تنفيذ M5 (مُنجز)

**المنهج:** بدل الفحص بالنظر (لا يقيس ولا يعمّم)، كُتب مدقّق آلي يفحص DOM في الوضع الداكن على 10 شاشات ويرصد موضوعياً: (أ) «الجزر الفاتحة» — عنصر مرئي مساحته >6000px² وإضاءة خلفيته >0.7؛ (ب) نصوصاً نسبة تباينها مع أقرب خلفية غير شفافة <2.5. المقاييس العرضية الأخرى تُقرأ برمجياً لا بالصورة.

**النتائج قبل الإصلاح:** 21 مشكلة تباين + جزيرة فاتحة واحدة، وشريط الحالة يُطبع.
**بعد الإصلاح: صفر جزر وصفر مشاكل تباين على الشاشات العشر.**

خمسة عيوب جذرية أُصلحت (كلها قائمة قبل هذا المشروع):

1. **19 توكناً «معلّقاً»** — الكود يكتب `var(--aseel-ok, #267346)` وأمثالها في **~280 موضعاً** لتوكنات **لم تُعرَّف قط في أي جلد**، فتسقط دائماً على القيمة الاحتياطية الصلبة: لا تستجيب للجلد ولا للوضع الداكن. وكانت الاحتياطيات نفسها متضاربة — `--aseel-ok` وحده مكتوب بأربعة أخضر مختلفة (`#267346`/`#2d7d46`/`#2e7d32`/`#27ae60`). عُرّفت كلها في الجلدين (الأصيل = القيمة السائدة حفاظاً على مظهره، الحديث = مشتقة). هذا وحده أصلح ~280 موضعاً دفعة واحدة.
2. **الشريط الكريمي في «معلومات المرجع»** (`AccountingJournalEntryPage.tsx:841`) — كان `style={{background:'var(--aseel-surface-2, #f4ede0)'}}` **inline**، أي لا كنس M4 يراه (ليس كلاساً) ولا CSS يتجاوزه. زال تلقائياً بتعريف `--aseel-surface-2`. هذا هو الشريط الذي رُصد في لقطات M2/M4.
3. **الدلالات داكنة جداً في الوضع الداكن** — `--color-success/danger/warning` لا تنقلب مع `.dark` (كتلة `.dark` تغطي الأسطح والنص فقط)، فأحمر `#dc2626` على `#1e293b` تباينه 2.9. رُفعت في `.dark[data-skin="modern"]` لدرجات فاتحة (`#4ade80`/`#f87171`/`#fbbf24`) تطابق عائلة `.dark .badge-*`.
4. **الشريط الجانبي غير مقروء في الداكن** — `text-[var(--color-primary)]` (#2563eb) فوق `bg-[var(--color-surface-3)]` (#334155) = تباين **2.0**. الحل ليس تفتيح `--color-primary` نفسه (يقتل تباين النص الأبيض فوق أزرار primary)، بل توكن منفصل `--color-primary-emphasis` **للنص فوق سطح ملوّن**: = primary في الفاتح، `#93c5fd` في الداكن (تباين 4.3). طُبّق في 6 مواضع بالشريط الجانبي فقط.
5. **شريط الحالة كان يُطبع** — قائمة إخفاء الطباعة تذكر `.aseel-statusbar-global` و`.aseel-doc-statusbar`، وكلاهما **اسم لا يستعمله `AseelDocumentShell`**؛ الكلاس الفعلي `.aseel-statusbar` كان غائباً. أُضيف.

**الفحوص الخمسة المطلوبة — كلها ناجحة بقياس لا بانطباع:**

| الفحص | النتيجة |
|-------|---------|
| الوضع الداكن (10 شاشات) | 0 جزيرة فاتحة · 0 تباين ضعيف |
| الطباعة | شريط الأدوات مخفي ✓ · شريط الحالة مخفي ✓ (بعد الإصلاح) · خط الشبكة 9pt=12px ✓ |
| الجوال RTL 375px | حزام الحقول عمود واحد ✓ · **صفر تجاوز أفقي** (scrollWidth=375=innerWidth) |
| الكثافة | ارتفاع الخلية 36px (مريح) ← 28px (مضغوط) ✓ |
| مقياس خط الشركة | 12px ← 15px عند تغيير `--aseel-fs` ⇒ الجلد الحديث يحترم إعداد AppearanceContext ✓ |

بوابات: `tsc` نظيف · البناء نظيف · `npm run test` ‏42/42 · Playwright ‏53/3 · `PARITY_MODE=compare` أخضر.

**قراران ينتظران المالك (لم يُنفَّذا — تغييرا سلوك لا مظهر):**
1. كتلة «Dark Aseel» الخاملة: تُربط بـ`.dark` ليعمل الداكن على الجلد الكلاسيكي، أم تُحذف؟
2. حفظ تفضيل الوضع الداكن في localStorage (سطران في `ThemeContext`) — حالياً يضيع مع كل تحديث صفحة.

---

### المرحلة M6 — بوابة الجودة والتسليم

```prompt
التسليم النهائي:
1) شغّل: npm run build ثم npm run test ثم npx playwright test كاملة ثم PARITY_MODE=compare — الأربعة خضراء إلزامياً. أي فشل ⇒ أصلح ثم أعد الأربعة من الصفر.
2) التقط لقطات after لكل الشاشات إلى e2e/parity-shots/modern/ وراجع يدوياً مقابل baseline: نفس العناصر، نفس التخطيط، مظهر أحدث فقط.
3) جولة يدوية موجهة (سجلها checklist في رسالة الإنجاز): إنشاء فاتورة مبيعات كاملة (أصناف عبر autocomplete + F-shortcuts + ترحيل + طباعة)، فاتورة شراء برسوم، صفقة استيراد (مراحل + دفعات)، قيد يومية، جرد، تنقل سجلات (الأول/التالي)، بحث عام، تبديل شركة/فرع، إشعارات، حاسبة، F11.
4) اقلب الافتراضي في styles/skin.ts إلى 'modern' مع بقاء خيار «كلاسيكي» في الإعدادات.
5) حدّث PROJECT_MAP.md: أضف قسم «تحديث الواجهة (FRONTEND_CHANGE)» وفق §6 من FRONTEND_CHANGE.md بما نُفذ فعلاً وقائمة ORPHANS.
6) commit نهائي برسالة واضحة. لا تحذف كتلة جلد aseel — تبقى fallback لإصدار كامل على الأقل.
```

**معيار قبول المشروع كله:** `parity compare` أخضر + الحِزم الاختبارية خضراء + الجولة اليدوية موثقة + مفتاح الرجوع للقديم يعمل.

#### سجل تنفيذ M6 (مُنجز — المشروع مكتمل)

**القراران المعلّقان حُسما بمنطق «المواقع الاحترافية»، وكلاهما كشف عيباً أعمق:**

1. **حفظ السمة + احترام تفضيل النظام.** `ThemeContext` صار يقرأ `localStorage['ktra.theme']` وإلا `prefers-color-scheme`، ويكتب عند التبديل، و`applyThemeOnBoot()` يُستدعى من `index.tsx` قبل الرندر (يمنع وميض الفاتح). **لكن الحفظ وحده لم يكفِ:** الاختبار أثبت أن السمة ما زالت تضيع بعد التحديث. السبب أن `App.tsx` كان يحوي **نظام سمة ثانياً مكرّراً** — حالة محلية + `useEffect` يضيف/يزيل كلاس `dark`. النظام ميت (`toggleTheme` فيه لا يُمرَّر لأي مكوّن، و`Header.tsx` الذي يستقبله **غير مستورد في أي مكان**) لكن الـ`useEffect` كان يزيل الكلاس عند كل تركيب فيُلغي ما طبّقه الإقلاع. حُذف النظام المكرّر بالكامل (الحالة والـeffect وtoggleTheme و8 أغلفة `className={theme}`)، فصار `ThemeContext` المصدر الوحيد.
2. **الجلد الكلاسيكي صار يدعم الوضع الداكن.** محدد كتلة «Dark Aseel» غُيّر من `[data-skin="aseel"][data-theme="dark"]` (لا يُطابَق أبداً) إلى `.dark[data-skin="aseel"]`، وأُضيفت لها نظائر داكنة لكل توكنات M5 الجديدة. النتيجة: الوضع الداكن يعمل على **الجلدين** لا الحديث وحده — قدرة لم تكن موجودة قبل هذا المشروع.

**قلب الافتراضي:** `styles/skin.ts` ← `DEFAULT_SKIN = 'modern'`. الكلاسيكي يبقى خياراً كاملاً في الإعدادات وكتلته في `index.css` **لا تُحذف** (شبكة أمان لإصدار كامل على الأقل).

**قلب الافتراضي كشف عيباً في الحارس نفسه:** فشلت `parity compare` بـ«`aseel-kit` و`aseel-sales`: entire view census is missing». التشخيص: الشاشتان تعملان تماماً (23 و21 زراً، صفر أخطاء) لكنهما تُرسمان **خارج `AppLayout`** فلا يوجد فيهما `main.app-content`؛ وشرط الانتظار كان `main.app-content, [data-skin="aseel"]` فيمرّ عندهما **بالصدفة** لأن `<html>` كان يحمل `data-skin="aseel"`. بعد قلب الافتراضي توقّف عن المطابقة فانتهى الانتظار بمهلة وسقطتا في `skipped`. الإصلاح: استثناء صريح `RENDERS_OUTSIDE_LAYOUT = {store, aseel-kit, aseel-sales}` لا يعتمد على الجلد — وهو **يقوّي** الحارس (الشاشتان تُحصَيان وتُقارَنان فعلاً بدل تخطّيهما).

**بوابة الجودة النهائية — كلها خضراء:**

| البوابة | النتيجة |
|---------|---------|
| `tsc --noEmit` | نظيف |
| `npm run build` | نظيف بلا تحذيرات |
| `npm run test` | 42/42 |
| `npx playwright test` | 53 passed · 3 skipped (مقصودة) |
| `PARITY_MODE=compare` | **أخضر تحت الافتراضي الجديد** — 69 شاشة بلا نقصان |
| مدقّق الداكن — الجلد الحديث | 0 جزيرة فاتحة · 0 تباين ضعيف |
| مدقّق الداكن — الجلد الكلاسيكي | 0 جزيرة · 0 تباين حقيقي (موضعان إيجابية كاذبة: نص أبيض على شريحة `rgba(255,255,255,.1)` فوق شريط داكن — التباين الفعلي ~11) |
| الافتراضي = modern | مؤكَّد باختبار |
| ثبات السمة عبر التحديث | مؤكَّد باختبار |

**PROJECT_MAP.md** حُدّث بقسم كامل: `[TECH_STACK]`, `[SYSTEM_FLOW]`, `[ARCHITECTURE]`, «مصائد مثبَّتة», `[ORPHANS & PENDING]`.

---

## 5) البروتوكول الرابع — استراتيجية الـ Logging

بساطة قصوى، غير حظري، للواجهة فقط (الباك إند لديه non-blocking logging جاهز ولا يُمس):

- ملف واحد `utils/uiLog.ts`: مستويات `debug/info/warn/error`؛ التنفيذ عبر `queueMicrotask(() => console[level](...))` كي لا يحجب الرندر؛ `debug/info` صامتان افتراضياً ويُفعّلان بـ `localStorage['ktra.uiLogLevel']`.
- يُستخدم فقط في نقاط هذا المشروع: تطبيق الجلد عند الإقلاع، تبديل الجلد، فشل قراءة localStorage. لا يُعمم على الكود القائم (No Feature Creep).
- يُنشأ ضمن M0-T3.

---

## 6) البروتوكول الخامس — الذاكرة الخارجية

`PROJECT_MAP.md` **موجود أصلاً في جذر المشروع (3152 سطراً) — يُضاف إليه قسم ولا يُنشأ ملف جديد**:

```markdown
## تحديث الواجهة — FRONTEND_CHANGE (2026-07)

[TECH_STACK]
React 19.2 · Vite 6.2 (بلا ترقية majors) · Tailwind 4.3 CSS-first · TS 5.8 · Playwright 1.60
نظام الجلود: :root[data-skin] على <html> · جلدان: aseel (كلاسيكي fallback) / modern (الافتراضي)

[SYSTEM_FLOW]
index.tsx → applySkinOnBoot() يضع data-skin على <html> من localStorage('ktra.uiSkin')
SettingsPage → setSkin() → dataset + حدث 'ktra:skin'
styles/index.css: قواعد .aseel-* هيكلية مشتركة تقرأ --aseel-* ← كل جلد يعرّف قيم المتغيرات
الداكن: data-theme="dark" لكل جلد · الطباعة N9-T5 · الجوال N9-T4 — بلا تغيير آلية

[ARCHITECTURE]
Skin Layer Swap: صفر تغيير منطق، أسماء كلاسات .aseel-* API مستقر (يتغير المحتوى لا الاسم)
حارس التكافؤ: e2e/feature-parity-census.spec.ts + e2e/parity-baseline.json (النقصان = فشل)
اللقطات: e2e/parity-shots/{baseline,modern}

[ORPHANS & PENDING]
- كتلة «N9-T3 Dark Aseel» خاملة: data-theme لا يُضبط أبداً (المبدّل يستخدم كلاس .dark) ⇒ جلد الأصيل بلا وضع داكن؛ القرار في M5
- الوضع الداكن لا يُحفظ: ThemeContext يبدأ 'light' دائماً بلا localStorage ⇒ يضيع مع كل تحديث صفحة (سلوك قائم، قرار للمالك)
- 1766 لوناً دلالياً (badges/حالات) لم تُوحَّد — المسار الصحيح ربطها بكلاسات .badge-* لا بتوكنات محايدة
- ~157 لوناً محايداً باقياً: عناصر داكنة بالتصميم + dark:bg-gray-700 على حقول تتولاها طبقة :where()
- حجم خط الجلد الحديث محكوم بـ AppearanceContext (inline على html) لا بالجلد — تغييره يتطلب تعديل منطق مقياس الخط
- حفظ اختيار الجلد على الخادم (TenantSettings.ui_skin) بدل localStorage — مؤجل عمداً
- الشاشات العامة (Landing/Login/Signup) خارج نطاق الجلود — حديثة أصلاً
- شاشات تخطّاها الإحصاء لغياب بيانات (قسم skipped في parity-baseline.json) — تُفحص يدوياً
- lucide-react v1 ترقية مؤجلة (أسماء أيقونات متغيرة)
- window.confirm في زر الخروج (AppLayout) لم يُحوّل إلى useConfirm — سلوك، خارج نطاق مشروع بصري
- ~~حذف كتلة جلد aseel نهائياً~~ — **مُلغى بقرار المالك 2026-08-25**: المظهر الكلاسيكي يبقى خياراً كاملاً في الإعدادات. المُسقَط هو **المرجعية** لا المظهر (§8)
- ~~**جزيرتا «أصيل» حيّتان داخل الجلد الحديث**~~ — **أُصلحت 2026-08-25 (M7-T0)**: `partners/PartnerProfilePage.tsx` و`sales/PaymentVoucherParts.tsx` تضعان `data-skin="aseel"` على نافذةٍ منبثقة، ومحدِّد التوكنات `[data-skin="aseel"]` (سطر 419) **بلا `:root`** فيُطابَق فعلاً ⇒ النافذتان ترثان الحدّ الزيتوني `#9e9c7e` والسطح `#ffffff` من اللوحة الكلاسيكية. وبلا مقابلٍ داكن (`.dark[data-skin="aseel"]` يلزمه `.dark` على العنصر نفسه وهو على `<html>`) ⇒ **نافذتان بيضاوان في الوضع الداكن**. حُذف الوسم من النافذتين فصارتا تتبعان جلد التطبيق وسمته. **يبقى دَيناً**: الأنماط inline فيهما تخالف قاعدة «Tailwind فقط» — مستقلّ عن M7
- **حارس التكافؤ معطّل منذ 2026-07-22** — شاشة «أنشئ شركتك الأولى» دخلت بعد تسجيل خط الأساس بيوم فعلّقت كل شاشة عليها (موثّق في `e2e/action-bar-dock.spec.ts`). لا يجوز البدء بـM7-T3/T4 قبل إحيائه: هو الشبكة الوحيدة التي تمسك «زرٌّ سقط» في إعادة تسميةٍ بـ7,541 موضعاً. تذكرة M7-T1
```

---

## 7) قواعد حاكمة للموديل المنفذ (اقرأها قبل كل تاسك)

1. **اقرأ قبل أن تلمس**: كل ملف يُقرأ كاملاً قبل تعديله. كتلة CSS تُفهم قبل تغييرها.
2. **مرحلة = commit**: لا تخلط تاسكات. أي فشل اختبار ⇒ أصلح داخل نفس التاسك قبل المتابعة.
3. **الترتيب إلزامي**: M0 قبل كل شيء — ممنوع أي تغيير بصري قبل اكتمال baseline.
4. **النقصان فشل**: أي عنصر/خانة/زر/تبويب/عمود يختفي من الإحصاء = توقف فوري وإصلاح.
5. **جلد aseel مقدس**: حتى M6 لا يتغير فيه بكسل — هو مرجع المقارنة وشبكة الأمان.
6. **الغموض = سؤال**: أي قرار تصميمي غير مغطى في §3.2 (لون دلالي، حالة خاصة) ⇒ اسأل المالك، لا تجتهد بصمت.
7. **ممنوعات مطلقة**: ترقية majors، مكتبات UI جديدة، تغيير نصوص عربية، تغيير سلوك، حذف كود، إعادة تسمية كلاسات، لمس services/ أو hooks/ أو contexts/ (إلا className) أو الباك إند.
8. **الأرقام**: أي عرض رقمي يمر عبر `utils/formatNumber` — لا تكسر قاعدة G1 أثناء أي تعديل.

---

## 8) المرحلة M7 — إسقاط «الأصيل» كمرجعية (سُجّلت 2026-08-25 · **نُقّحت بقرار المالك في اليوم نفسه**)

> ### ⚠ تنقيح حاكم — اقرأه قبل أي شيء
>
> النسخة الأولى من M7 قرأت الطلب «تخلّص من تصميم الأصيل» على أنه **حذف المظهر**،
> وصحّح المالك في اليوم نفسه:
>
> > «ما قصدي تقيّم مظهر الواجهة الكلاسيكي (الأصيل) — هذا خلّيه كمظهر كلاسيكي.
> > أنا بدي تلغي إنه أنت مرجعيتك الأصيل. أما لسّا بدي خيار أقدر أغيّر الواجهة
> > من حديث لكلاسيكي.»
>
> فالمطلوب **إسقاط المرجعية لا حذف المظهر**. وعليه:
>
> - **T2 (حذف الجلد الكلاسيكي) — مُلغاة نهائياً.** الجلدان يبقيان، ومبدّل
>   المظهر في الإعدادات يبقى كامل الوظيفة.
> - **يُشطب من `[ORPHANS & PENDING]` أعلاه** بندُ «حذف كتلة جلد aseel نهائياً»:
>   لم يعد بنداً مؤجَّلاً بل **قراراً بعدم الفعل**.
> - «الأصيل» **اسمُ برنامجٍ آخر**، فيخرج من واجهة منتَجنا ومن بروتوكولات عملنا،
>   ويبقى **المظهر** تحت اسمه الوصفي «كلاسيكي».

### ما نُفِّذ فعلاً (2026-08-25)

**الطبقة 1 — البروتوكولات (المرجعية):** شُطب «الأصيل» من قائمة *Named references*
في `.claude/skills/engineering-protocol/SKILL.md` ومن المراجع المعتمدة في
`.claude/skills/notes-to-multica/SKILL.md`، وحلّ محلّه نصٌّ صريح **يمنع**
الاستشهاد به لتبرير شكل شاشةٍ جديدة. و`docs/aseel_reference/README.md` صار يعلن
نفسه **أرشيفاً تاريخياً لا مرجع تصميم**، يُستشار بطلب صريح فقط — ولم يُحذف حرف
منه: هو ذاكرة *لماذا* اتُّخذت قرارات قديمة.

**الطبقة 2 — الاسم الظاهر للمستخدم:** «كلاسيكي (الأصيل)» في مبدّل المظهر صارت
«كلاسيكي»؛ و«حاسبة الأصيل» صارت «الحاسبة»؛ وتسميتا الشاشتين في `VIEW_LABELS`
صارتا «مكوّنات الواجهة» و«فاتورة المبيعات (كلاسيكي)». **القيمة المخزّنة `aseel`
لم تُمسّ** — مفتاحٌ قائم في متصفّحات المستخدمين، وهجرته تكسر تفضيلاً محفوظاً
مقابل صفر مكسب؛ تُؤجَّل إلى T3 حيث تُكتب مع قراءةٍ متوافقة.

**T0 — الجزيرتان:** حُذف `data-skin="aseel"` من نافذتَي `PartnerProfilePage.tsx`
و`PaymentVoucherParts.tsx`. **السبب الجذري**: محدِّد التوكنات الكلاسيكية في
`styles/index.css:419` مكتوب `[data-skin="aseel"]` **بلا `:root`** — فوسمٌ محليّ
على أي `div` يُطابقه فعلاً. النافذتان كانتا ترثان الحدّ الزيتوني `#9e9c7e`
والسطح `#ffffff` داخل الجلد الحديث، **وبلا مقابلٍ داكن** (`.dark[data-skin="aseel"]`
يلزمه `.dark` على العنصر نفسه وهو على `<html>`) ⇒ نافذتان بيضاوان في الوضع
الداكن. الإصلاح سطران فقط: متغيّرات `--aseel-*` معرَّفة على `<html>` في
**الجلدين** (الحديث يوجّهها إلى `--color-*`)، فحذفُ الوسم يجعل النافذتين تتبعان
جلد التطبيق وسمته بلا لمس أي نمط.

### الجرد المُقاس (‏2026-08-25، من الملفات نفسها)

| المقدار | العدد |
|---|---:|
| `styles/index.css` كلّه | 2,401 سطراً |
| قواعد `:root[data-skin] .aseel-*` (هيكلية، مشتركة بين الجلدين) | 248 |
| قواعد `:root[data-skin="modern"]` (تجاوزات الجلد الحديث) | 110 |
| توكنات كتلة `[data-skin="aseel"]` (القيم الكلاسيكية) | ~60 |
| مواضع `aseel-*` في `components/**.tsx` | 7,541 في 173 ملفاً |
| `components/aseel/` | 23 ملفاً · 4,004 أسطر |

الثقل الأكبر في `components/procurement` (3,655) ثم `sales` (838) ثم
`accounting` (615) — أي أن أي هجرة تسميةٍ يدوية تلمس أثقل شاشات المال في النظام.

---

### التذاكر — نُفِّذت كلها (2026-08-25)

> **القاعدة التي حكمتها:** `.aseel-*` **لم تكن مظهراً** — هي **عقد بنية
> الشاشات** يقرأ توكنات كلا الجلدين. إعادة تسميتها لم تُغيّر بكسلاً واحداً ولم
> تحذف المظهر الكلاسيكي؛ تنظيف هويّة بحت، وخطرها كان كلّه في الشمول — ولذلك
> جرت بآلةٍ تحت حارسٍ حيّ، لا بيد.

#### M7-T1 — إحياء حارس التكافؤ *(منفَّذة 2026-08-25)* — **والسبب لم يكن ما ظنّته التذكرة**

التذكرة قالت «معطّل منذ 2026-07-22 لأن شاشة أنشئ شركتك الأولى تعلّق كل هدف».
هذا كان صحيحاً **وقد أُصلح سابقاً**: المُحاكي في `installAuthenticatedApiMocks`
يردّ عضويةَ شركة بالفعل، ومعه استثناء الشاشات التي تُرسم خارج `AppLayout`.
تشغيلُ `PARITY_MODE=compare` مرّ على **الشاشات الثماني والسبعين كلّها** وأنتج
فشلاً واحداً:

```
accounting-cheques.tableHeaders: missing "تاريخ الاستحقاق"
```

والعمود **حاضر** (`AccountingChequesPage.tsx`، `key: "due_date"`). السبب أن
`AseelDenseTable` تحقن مثلّث الفرز **داخل `<th>`** لعمود الفرز النشط، والشاشة
تفتح على `ordering = "-due_date"` — فنصّ الرأس المجموع صار `تاريخ الاستحقاق▼`
ولم يطابق خط الأساس. أي أن الحارس كان **هشّاً**: أيُّ عمودٍ يصير عمودَ الفرز
النشط يُبلَّغ عنه «مفقوداً» وهو حاضر، فيغرق النقصان الحقيقي في ضجيج كاذب.

**الإصلاح في ثلاث نقاط:**

1. **الزخرفة تُعلَن زخرفةً**: مثلّث الفرز صار `aria-hidden="true"` في
   `AseelDenseTable.tsx` و`GroupedItemsTable.tsx`، وحلّت محلّه `aria-sort` على
   `<th>` — فالمعلومة لم تُفقد بل **وصلت قارئ الشاشة لأول مرّة**؛ المثلّث لم
   يكن يصله أصلاً.
2. **الحارس يقرأ النصّ بلا زخرفة**: `collectCensus` تستنسخ العنصر وتحذف
   `[aria-hidden="true"]` قبل قراءة نصّه.
3. **الحكم خرج من المتصفّح**: `missingValues`/`compareBaselines`/`sortedBaseline`
   انتقلت إلى `utils/parityCensus.ts` (نقيّة بلا DOM) ويستوردها الـspec. فصار
   **إثبات أن الحارس يحمرّ حين يسقط زرّ** يكلّف أجزاء الثانية في
   `utils/parityCensus.test.ts` بدل ثلاث عشرة دقيقة — ومن لا يستطيع إثبات حمرة
   حارسه لا يملك حارساً بل طمأنينة. والزيادة صارت `additions` مفصولةً صراحةً عن
   `failures`: الميزة الجديدة ليست عطلاً.

#### M7-T3 — هجرة التسمية `aseel-` → `ktra-` *(منفَّذة 2026-08-25)*

**8,660 موضعاً في 201 ملفاً بـcodemod واحد** (`aseel-` ← `ktra-`)، تشمل ثلاثة
أنواع من الرموز تحمل السابقة نفسها: أصناف CSS (`.aseel-toolbtn`) ومتغيّراتها
(`--aseel-border`) وخصائص DOM (`data-aseel-key`). النظرة الأمامية السالبة حَمَت
`aseel-kit`/`aseel-sales` — ليستا صنفين بل قيمتَي `AppView` ومسارَي تنقّل،
وقرارهما في T4.

**وقيمة الجلد نفسها**: `'aseel'` ← `'classic'` في `styles/skin.ts` و`index.css`
والإعدادات — **بقراءةٍ متوافقة**: `getSkin()` يقبل القيمة القديمة ويُرجعها
`classic`، فمن اختار الكلاسيكي لا يُعاد إلى الحديث بصمت عند أول تحديث.
والجلد **باقٍ خياراً كاملاً**: هذه إعادة تسمية لا حذف.

**البرهان**: `PARITY_MODE=compare` أخضر على 78 شاشة — صفر زرّ أو حقل أو عمود
سقط. و`npm run build` و`tsc` نظيفان، و504 اختبار وحدة خضراء.

#### M7-T4 — `components/aseel/` تصير `components/kit/` *(منفَّذة 2026-08-25)*

**قرار التسمية، وسببه:** الخطة الأولى قالت `components/document/` بسابقة
`Document`. المجلد فيه حاسبة وقائمة سياق ونافذة عائمة وحقل تاريخ وجدول تقارير —
و«مستند» يكذب عليها. الاسم الصادق **عُدّة** (`kit`). والسابقة تبقى ضرورية لا
زينة: `Spinner` و`EmptyState` موجودان فعلاً في `components/ui/`، فالاسم المجرّد
يصطدم بهما — وهو سبب وجود السابقة أصلاً.

```
components/aseel/       → components/kit/        AseelDenseTable → KitDenseTable
ui/AseelDatePicker      → ui/KitDatePicker       useAseelKeymap  → useKitKeymap
SalesInvoiceAseelStory  → SalesInvoiceKitStory   ItemFormAseel   → ItemForm
e2e/aseel-*.spec.ts     → e2e/kit-*.spec.ts
```

`ItemFormAseel` لم يصر `ItemFormKit`: «الأصيل» فيه كانت تعني «نموذج الصنف بنمط
الأصيل» وهو نموذج الصنف **الوحيد** — فاسمه الصادق `ItemForm`، ولا يصطدم بشيء.

**قيمتا `AppView`**: `aseel-kit` ← `ui-kit` و`aseel-sales` ← `sales-classic`،
**والمساران القديمان يبقيان مقبولين** في `App.tsx`: إعادة تسمية داخلية لا يجوز
أن تكسر رابطاً في مفضّلة أحدهم. ومفاتيح خط الأساس ولقطاته تبعت الاسم الجديد.

**ومفاتيح التخزين المحلي رُحِّلت لا مُسحت** — `utils/legacyStorageKeys.ts`:
`aseel_table_widths_*` و`aseel_table_row_heights_*` و`aseel_calc_history` مفاتيح
**حيّة في متصفّحات المستخدمين** (عروض أعمدة ضبطها المستخدم بيده، وسجلّ حاسبته).
تغييرُ الاسم بلا ترحيل يعني اختفاء ضبطٍ بُني عبر شهور **بصمت** عند أول تحديث.
الترحيل يجري مرّةً عند الإقلاع قبل أن يقرأ أي جدول عرضَ أعمدته، ولا يدهس مفتاحاً
جديداً موجوداً، ولا يُسقط الإقلاع إن كان التخزين محظوراً.

**الباقي من كلمة `aseel` في `frontend_v2/` مقصودٌ كلّه**: ثابتا التوافق
(`LEGACY_CLASSIC`, `LEGACY_PREFIX`) واختباراتهما، وتعليقاتٌ تشرح سببَ ما جرى،
ومراجعُ إلى الأرشيف `docs/aseel_reference/` — وهو باقٍ بقرار المالك.

**وقاعدةٌ تعلّمتُها بحمرتين كاذبتين، كلتاهما من صنعي**:

الحارس يقود **متصفّحاً حقيقياً على خادم تطوير حيّ** بمهلٍ زمنية (15ث للرسم،
ثم حلقة استقرار 10ث). فكلّ ما يزعزع الحيّ أو الزمن يُنتج «شاشةً فارغة» تُقرأ
نقصاناً — والنقصان فشل بالتعريف.

| الحمرة | ما فعلتُه | ما ظهر | الحقيقة |
|---|---|---|---|
| بعد T4 | عدّلتُ ملفاً في `frontend_v2/` أثناء التشغيل | `sales-classic` بلا أزرار ولا حقول | Vite أعاد التحميل تحت القياس — الشاشة سليمة بـ24 زرّاً |
| التشغيل الختامي | شغّلتُ `pytest -q -n auto` على كل الأنوية | `sales-quotations` و`credit-debit-notes` فارغتان | تجويع معالج: تأخّر تحميل الحِزَم الكسولة عن المهلة — الشاشتان ترسمان ما ينتظره خط الأساس بالضبط |

**القاعدة**: وحارسُ التكافؤ يعمل، لا يُعدَّل ملفٌ تحت `frontend_v2/` **ولا
يُشغَّل حِملٌ ثقيل على الجهاز**. وقبل تصديق أي حمرة منه: افتح الشاشة المتّهمة
مباشرةً وعُدّ أزرارها — دقيقةٌ واحدة تفصل عطلاً حقيقياً عن ضجيجٍ صنعتَه بيدك.

#### الدَّين المستقلّ — أنماط inline في النافذتين *(مُسدَّد 2026-08-25)*

63 موضع `style={{…}}` في `PaymentVoucherParts.tsx` (44) و`PartnerProfilePage.tsx`
(19) صارت أصنافَ Tailwind — **صفر `style={` باقٍ في الملفّين**.

**والفخّ الذي كاد يبتلع التحويل**: القاعدتان `thead th` و`tbody td` في
`styles/index.css` **خارج الطبقات**، وأيّ قاعدة خارج الطبقات تغلب كل أدوات
Tailwind مهما بلغت دقّتها (‏`@import "tailwindcss"` يضعها في `@layer utilities`).
فتحويل `<td style={{padding:"2px"}}>` إلى `className="p-0.5"` كان **سيسلّم
الخلية إلى `0.375rem 0.75rem`** وترتفع صفوف شبكة الشيكات ثلاثة أضعاف. الحلّ صنفٌ
خارج الطبقات أيضاً يغلب بالدقّة (0,1,1 مقابل 0,0,2): `.ktra-compact-grid` بحشوة
4px و`--tight` بـ2px لشبكة الشيكات وحدها.

**قياسٌ مباشر يثبت التكافؤ بالبكسل** (لا ثقة): جدولٌ بلا صنف يعطي `6px 12px` —
وهو ما كان سيحدث بتحويل ساذج؛ و`.ktra-compact-grid` يعطي `4px` (= inline القديم
في منتقي السندات)؛ و`--tight` يعطي `2px` (= inline القديم في شبكة الشيكات).

---

## 9) وُجِد في الطريق — قائمٌ قبل هذا العمل، ولم يُدمج فيه

> قاعدة البروتوكول: الخطر غير النازف **تذكرةٌ مستقلّة**، لا يُهرَّب في diff لا
> علاقة له به. وكلاهما **أُثبت أنه سابقٌ للتغيير** لا ناتجٌ عنه.

### F-1 — `/sales/customer-payments` بالرابط المباشر يعرض لوحة التحكم

**الأثر**: من يضغط F5 على شاشة «دفعات العملاء»، أو يفتحها من مفضّلة، أو يفتحها في
تبويب جديد — يُقذف إلى لوحة التحكم. **والرابط يبقى `/sales/customer-payments`
في شريط العنوان**، فلا شيء يقول للمستخدم ما حدث. شاشةٌ مالية.

**البرهان**: بمحاكي الصلاحيات نفسه الذي يستعمله حارس التكافؤ، عنوانُ الصفحة
المعروضة «توزيع المهام» وعددُ أزرار «سند قبض جديد» صفر، والرابط لم يتغيّر.

**والأخطر**: `e2e/parity-baseline.json` يحمل تحت مفتاح `sales-customer-payments`
حصيلةَ **لوحة التحكم** لا الشاشة (‏14 زرّاً بنصوص فارغة وصفر عمود جدول) — أي أن
الحارس **أعمى على هذه الشاشة** منذ تسجيل خط الأساس. أيُّ زرّ يسقط منها لن يُمسك.

**سابقٌ للتغيير**: خط الأساس مسجَّل 2026-07-21، قبل هذا العمل بشهر.

### F-2 — `e2e/default-cash-account.spec.ts` أحمر منذ ما قبل هذا العمل

**البرهان القاطع**: `git stash push -u -- frontend_v2` (شجرةٌ على HEAD تماماً،
بمجلد `components/aseel/` عائداً) ثم تشغيل الاختبار ⇒ **أحمر**. ثم `pop` وتحقّق
من الاسترجاع. لم يُلمس الملفّ في هذا العمل (‏`git status` نظيف عليه).

**سببه المباشر** F-1 أعلاه: الاختبار ينتقل إلى `/sales/customer-payments` ثم
ينتظر نداء `/api/sales/settings/current/` الذي لا يقع لأن الشاشة لا تُرسم.
وحتى لو رُسمت، يطلب الاختبار زرّاً اسمه **«دفعة جديدة»** — وهو نصٌّ **غير موجود
في المستودع كلّه**؛ الزرّ الفعلي «سند قبض جديد (Ctrl+Ins)». فالاختبار متأخّر عن
شاشته بأكثر من إعادة تسمية.

**علاجهما واحد**: إصلاح F-1 أولاً، ثم إعادة تسجيل خط الأساس لهذه الشاشة وحدها،
ثم تحديث نصّ الزرّ في الاختبار. تذكرةٌ مستقلّة لأن F-1 مسّ للموجّه في `App.tsx`
لا علاقة له بإعادة تسمية.

---

**M7 اكتملت**: T0 · T1 · T2 (مُلغاة بقرار) · T3 · T4 · الدَّين المستقلّ.
**وبقيت مفتوحةً بقرار**: F-1 و F-2.
