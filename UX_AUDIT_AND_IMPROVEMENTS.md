# K.T.R.A — تدقيق تجربة المستخدم وخطة التحسينات الشاملة
### رحلة: صفقة → مورد → منتج → شحنة → تخليص → فاتورة دولية
**التاريخ:** 2026-07-17 (جلسة 1) + 2026-07-18 (جلسة 2 — دفع الرحلة فعلياً حتى فاتورة دولية حقيقية) · **المنهجية:** جولة حيّة كمستخدم جديد على `localhost:3000` (6 صفقات حقيقية بموردين ومنتجات جدد عبر جلستين) + تدقيق كود مقابل الأعراض المرصودة.
**الدور:** Staff Engineer / Tech Lead — لغة تقنية مكثّفة، أولويات قابلة للتحقّق (Verifiable Goals).

> **تحديث الجلسة 2 (2026-07-18):** انظر القسم 8 أدناه. أُغلقت U11–U16 وG13–G17،
> وأُكملت D-0109، ثم نُفّذت رحلتان جديدتان D-0112 وD-0113 حتى ثلاث فواتير دولية حقيقية
> إضافية (`INV-0004`–`INV-0006`) عبر الواجهة.

---

## 0. الملخّص التنفيذي

النظام **ناضج وظيفياً**: رحلة استيراد كاملة (6 مراحل)، محرّك توزيع Decimal يطابق للقرش، حساب تكلفة وصول بالشيكل، تتبّع خلفي (بند→صفقة→شحنة→تخليص→نقل)، وحسابات حيّة ممتازة. لكن **تجربة المستخدم الجديد هشّة**: تعثّرت في أول 5 دقائق عند أبسط مهمة (إضافة منتج لصفقة).

الحكم بإيجاز: *«محرّك خلفي احترافي داخل واجهة بلغة برنامج محاسبي قديم (Aseel-style)».* الفجوة ليست في القدرة بل في **الوضوح، وتوحيد المسارات، ومعالجة الأخطاء**.

**أخطر 4 نقاط (تُعالَج فوراً):**
1. **فخّ ربط المنتج (Blocker):** الكتابة في «اسم الصنف» ثم «إضافة كصنف جديد» تُظهر الاسم لكن لا تُنشئ/تربط منتجاً → الحفظ يفشل برسالة خام `{"product":["هذا الحقل مطلوب."]}`.
2. **زر «شحنة من الصفقات» طريق مسدود:** بارز في القائمة لكنه يرفض الصفقات الجديدة («قدّم الصفقة إلى تم الشحن للوكيل أولاً») بلا إرشاد.
3. **عطل توجيه:** زر «الخطوة التالية / ابدأ الشحن الدولي» لا يفتح رحلة الاستيراد إلا بعد إعادة تحميل كاملة (SPA route لا يُطابق داخلياً).
4. **ازدواج النموذج:** نموذج الصفقة يعرض مجموعتَي حقول (شبكة علوية + تبويب سفلي) لنفس البيانات، وحقل المورد لا يتزامن بينهما.

---

## 1. احتكاكات تجربة المستخدم (مرتّبة حسب الخطورة)

| # | الخطورة | الموضع | العَرَض المرصود | الإصلاح المقترح |
|---|---------|--------|------------------|------------------|
| U1 | 🔴 P0 | نموذج الصفقة — بند المنتج | «إضافة كصنف جديد» من حقل الاسم لا يربط منتجاً؛ الحفظ يفشل برسالة JSON خام غير مترجمة | مسار إنشاء واحد يُنشئ Product فعلياً ويربط `product_id` قبل الحفظ؛ منع الحفظ وإبراز السطر الناقص برسالة عربية |
| U2 | 🔴 P0 | قائمة الصفقات | «شحنة من الصفقات» يرفض الصفقات الجديدة برسالة تقنية وزر إنشاء مُعطّل بلا سبب | إمّا إخفاء الزر حتى تجهز صفقات، أو تحويله لمعالِج يقدّم الصفقة للمرحلة المطلوبة تلقائياً |
| U3 | 🔴 P0 | زر «الخطوة التالية» داخل الصفقة | لا يفتح `/import-flow/new` إلا بعد reload كامل؛ المرحلة لا تتقدّم | إصلاح مطابقة المسار (أدناه B-Route) واستخدام `navigate()` الفعلي |
| U4 | 🟠 P1 | نموذج الصفقة | مجموعتان من نفس الحقول (شبكة علوية legacy + تبويب سفلي حديث)؛ حقل المورد يعرض `#296` بعد الحفظ ولا يتزامن مع الأسفل | توحيد على النموذج الحديث (تبويبات)، إزالة الشبكة العلوية أو جعلها للعرض فقط |
| U5 | 🟠 P1 | حفظ الشحنة | «لا يمكن لهذا الحقل أن يكون فارغاً!» بلا تحديد الحقل ولا إبرازه | تحقّق حقلي (field-level) يبرز الحقل ويمرّر رسالة مخصّصة له |
| U6 | 🟠 P1 | التنقّل | لا يوجد بند «الصفقات» في قائمة «المشتريات»؛ الوصول عبر بطاقة KPI فقط | إضافة «الصفقات» و«الشحنات/رحلة الاستيراد» كبنود صريحة في القائمة |
| U7 | 🟡 P2 | عام | مصطلح واحد بأربعة أسماء: مورد/مصنع/مصنّع/Supplier؛ ومعرّفات مختلطة (UUID طويل مقابل #7249) | قاموس مصطلحات موحّد + إخفاء المعرّفات التقنية أو توحيد نمطها |
| U8 | 🟡 P2 | نموذج الصفقة | تواريخ mm/dd/yyyy في واجهة عربية RTL؛ شريط أدوات بنمط سطح مكتب (F2/F12) غير مكتشف | صيغة تاريخ محلية + إبراز الاختصارات أو استبدالها بأزرار واضحة |
| U9 | 🟡 P2 | الشريط العلوي | ~9 أيقونات بلا تسميات ولا tooltips (وصولية) | إضافة `aria-label` + tooltips لكل أيقونة |
| U10 | 🟡 P2 | قائمة الصفقات | عمودا «الحالة» و«المرحلة» متشابهان ومربكان؛ زر الحذف أول أيقونة (خطر) | دمج/توضيح العمودين؛ تأكيد الحذف؛ إبعاد زر الحذف |

**إيجابيات تستحق الحفاظ عليها:** مودال إضافة المورد نظيف وحديث · الحسابات الحيّة فورية ودقيقة · لوحة «الخطوة التالية» + خريطة الرحلة العمودية (تلقائي مقابل يدوي) تصميم ممتاز · شريط الرحلة 6-خطوات واضح · نصوص إرشادية جيدة («حالة الدفع مستقلة عن مرحلة الشحن»).

---

## 2. تحسينات الفرونت-إند (Frontend)

### 2.1 توحيد نموذج الصفقة (يعالج U1, U4)
- **الجذر (كود):** `LogisticsDealItemSerializer` يجعل `product` حقلاً إلزامياً (ForeignKey `PROTECT`) بلا `required=False`. أي سطر بلا `product_id` يُرفض. مسار «اسم الصنف → إضافة كصنف جديد» يملأ `name_snapshot`/`catalog_number` فقط بلا إنشاء Product → الرفض حتمي.
- **الإصلاح:** جعل مُنتقي المنتج (`...` → «إضافة صنف سريع») هو **المسار الوحيد**؛ حذف مسار الكتابة-الحرة المعطوب أو تحويله ليستدعي نفس منطق الإنشاء (POST product ثم ربط `product_id` في السطر). ضمان أن «إضافة صنف» **تملأ السطر الحالي** لا أن تُدرج سطراً جديداً.
- تحقّق أمامي قبل الإرسال: منع الحفظ إن وُجد سطر بلا `product_id`، مع إبراز السطر ورسالة عربية.

### 2.2 معالجة أخطاء الـAPI (يعالج U1, U5)
- طبقة موحّدة تحوّل أخطاء DRF (`{"field":["msg"]}`) إلى رسائل عربية مربوطة بالحقل بدل عرض JSON الخام في toast. خريطة `field → label عربي` + fallback عام مهذّب.
- تحقّق حقلي في نماذج الشحنة/الفاتورة يبرز الحقل الناقص (`aria-invalid` + حدّ أحمر + رسالة أسفله).

### 2.3 إصلاح التوجيه (يعالج U3)
- في `App.tsx`: مطابقة `^\/import-flow\/(.+)$` + منطق `navigate("view")` لا يتعاملان بثبات مع أول تنقّل SPA إلى `/import-flow/new?...` (سقط إلى قائمة الصفقات وتطلّب reload). وحّد التنقّل على `react-router` `navigate()` الفعلي بدل إعادة بناء المسار يدوياً، وتأكّد أن `/import-flow/new` مُسجّل صراحةً ويقرأ `?join_deal` بعد mount.

### 2.4 حجم الحزمة والأداء
- من PROJECT_MAP: `main` ≈ **1128kB خام / 271kB gzip** وتحذير `chunk > 500k` قائم، ووجود `eval` في الحاسبة.
- **إجراءات:** code-splitting أعمق للمسارات الثقيلة (import-flow، الفواتير)، تحليل الحزمة (`rollup-plugin-visualizer`)، إزالة `eval` (يتعارض مع CSP صارم)، وتفعيل ضغط brotli.

### 2.5 التصميم والوصولية (يعالج U8, U9)
- استبدال لغة «سطح المكتب» (الأول/السابق/التالي/الأخير/تخزين F12) بأنماط ويب حديثة (أزرار مسمّاة، حفظ تلقائي/صريح واحد).
- `aria-label` + tooltips لكل أيقونة في الشريط العلوي؛ تباين ألوان AA؛ أهداف لمس ≥ 44px لأيقونات الإجراءات في الجداول (حالياً متلاصقة).
- صيغة تاريخ محلية عبر `Intl` مع دعم RTL كامل.

### 2.6 نظام تصميم موحّد
- توثيق مكوّنات مشتركة (حالات فارغة، أخطاء/Retry، Toast، Modal، حقول) — بعضها موجود (`AseelErrorState`) لكن غير معمّم. اعتماد Design Tokens (مسافات/ألوان/خطوط) عبر Tailwind theme لمنع الانجراف البصري.

---

## 3. تحسينات الباك-إند (Backend)

### 3.1 عقد المنتج في سطر الصفقة (الجذر التقني لـU1)
- `LogisticsDealItemSerializer.fields` يتضمّن `product` (FK إلزامي). خياران: (أ) إبقاؤه إلزامياً + إجبار الواجهة على الإنشاء-ثم-الربط (مفضّل، يحفظ سلامة المخزون)، أو (ب) endpoint ذرّي `POST /deals/{id}/items/` يُنشئ Product ويربطه في معاملة واحدة (atomic) فيستحيل وجود سطر يتيم. أرفق رسالة تحقّق عربية على مستوى الحقل.

### 3.2 توحيد رسائل الأخطاء وi18n خادمياً
- توحيد `ValidationError` برسائل عربية متسقة + رمز خطأ (`code`) لتسهيل الربط الأمامي؛ تجنّب تسريب أسماء الحقول التقنية.

### 3.3 الأداء: N+1 والترقيم
- PROJECT_MAP يوثّق N+1 مُقاساً في قوائم `deals`/`shipments` ذات الحمولة المتداخلة، وترقيم مؤجّل. **الهدف:** list serializers خفيفة + `select_related/prefetch_related` مقيّدة بالمستأجر، ثم ترقيم خادمي **بعد** نقل البحث/التاريخ/الحالة للخادم (كي لا تختفي مستندات مالية قديمة كما حذّر PROJECT_MAP).

### 3.4 التسجيل غير الحاجب (بروتوكول 4 — غير منفّذ بعد)
- `[ORPHANS & PENDING]` يذكر «تسجيل غير حاجب (QueueHandler/QueueListener) — يُضاف». تنفيذه: `QueueHandler` + `QueueListener` بمستويات أساسية فقط، بلا أثر على مسار الطلب.

### 3.5 الهجرات والاتساق
- هجرات معلّقة بانتظار تطبيق المالك: **0052** (`DeleteModel LogisticsExpense`)، **0053** (حقول آمنة). خطة: تأكيد صفر صفوف إنتاج ثم تطبيق، مع بقاء `transit_journal` (حارس الشحنة المرحّلة) عمداً.
- الحفاظ على الثابت: `python manage.py test --settings=core.test_settings` (≥308) + `makemigrations --check --dry-run` = No changes قبل أي commit موديل/هجرة (قاعدة PROJECT_MAP الصارمة).

### 3.6 انجراف الإصدارات
- مثبّت محلياً Django `6.0.3`/DRF `3.17.1` بينما `requirements` يثبّت `6.0.1`/`3.16.1`. توحيد الأرقام أو تثبيت السلسلة (تجنّب مفاجآت الإنتاج). صفر تبعيات جديدة (بروتوكول 3 — جراحي).

---

## 4. الأمان (Security)

| البند | الحالة | الإجراء |
|-------|--------|---------|
| أسرار كـdefaults في `settings` | موثّق كـproduction-access-blocked | تدوير قيم الإنتاج + `env` أولاً ثم حذف الـdefaults بتنسيق إنتاج |
| `eval` في الحاسبة | قائم | إزالته (يمنع CSP صارم) واستبداله بمُقيّم آمن |
| رؤوس أمان | غير مؤكّدة | CSP، HSTS، `X-Content-Type-Options`, `Referrer-Policy` |
| تسريب معرّفات/أخطاء | مرصود (رسالة DRF خام، `#296`) | إخفاء التفاصيل التقنية من واجهة المستخدم |
| عزل المستأجر | مُصلَح جزئياً (M1) | مراجعة كل ViewSet لضمان فلترة `tenant` (سبق أن سرّب ملخص المخزون منتج مستأجر آخر) |

---

## 5. تحسينات «الموقع التجاري» (Commercial / New-User UX)

هدف المالك حرفياً: *«يدخل ويحس حاله مستخدم جديد بدو سهولة وبدوش وجع راس»*. المطلوب تحويل النظام من «أداة خبراء» إلى «منتج يقود المستخدم».

1. **التنقّل حسب المهمة لا حسب الجدول:** أضف «الصفقات» و«رحلة الاستيراد» كبنود صريحة؛ زر «+ صفقة جديدة» عالمي في الشريط العلوي (إجراءات سريعة موجودة — اربطها بالمهام الأساسية).
2. **معالِج (Wizard) للمستخدم الجديد:** «أنشئ أول صفقة استيراد» بخطوات مرقّمة (مورد → منتجات → مراجعة → التالي) يخفي عشرات الحقول المتقدّمة خلف «تفاصيل إضافية» (الطيّة موجودة — وسّع استخدامها).
3. **حالات فارغة موجِّهة:** بدل زر ميت في «شحنة من الصفقات»، اعرض «لا صفقات جاهزة بعد — [جهّز صفقة الآن]».
4. **قوائم تدريجية (Progressive Disclosure):** الأساسي (مورد، بنود، إجمالي) ظاهر؛ الشهادات/البوليصة/الضمان مطويّة افتراضياً.
5. **مصطلحات بشرية موحّدة:** «مورد» فقط (لا مصنع/مصنّع/Supplier معاً)، وإخفاء المعرّفات التقنية.
6. **مؤشّر تقدّم دائم للرحلة:** شريط الـ6-خطوات ممتاز — اجعله ظاهراً أيضاً من داخل الصفقة ومن القائمة (نقرة واحدة لمعرفة «أين أنا؟»).
7. **جولة تعريفية أول دخول (Onboarding tour)** اختيارية + مركز مساعدة سياقي («ما الجديد» موجود — استثمره).
8. **الثقة:** رسائل نجاح/خطأ متسقة (النجاح جيد بالفعل: «تم إنشاء الصفقة بنجاح برقم D-0106»)، وتأكيد قبل الحذف.

---

## 6. خطة العمل — معالم قابلة للتحقّق (Milestones)

> كل معلم ينتهي بـ«هدف قابل للتحقّق» (اختبار/بناء/سيناريو e2e). يُلتزم بالثابت: Django tests خضراء + `makemigrations --check` = No changes + `tsc` صفر + `vite build` ناجح.

### 🔴 M-P0 — إزالة الحواجز (أسبوع 1)
- **G1:** مسار إنشاء منتج واحد يربط `product_id` فعلياً. *تحقّق:* سيناريو e2e «صفقة جديدة → منتج جديد → حفظ» ينجح بلا خطأ `product`.
- **G2:** رسائل خطأ عربية حقلية (لا JSON خام). *تحقّق:* حفظ سطر بلا منتج يُبرز السطر برسالة عربية، ولا يظهر `{"product":...}`.
- **G3:** إصلاح توجيه «الخطوة التالية» بلا reload. *تحقّق:* نقرة الزر تفتح `/import-flow/new` وتضمّ الصفقة، والمرحلة تتقدّم — e2e أخضر.
- **G4:** زر «شحنة من الصفقات» لا يصل لطريق مسدود (إخفاء أو معالِج). *تحقّق:* مستخدم جديد يصل لشحنة دون رسالة تقنية.

### 🟠 M-P1 — توحيد النموذج والتحقّق (أسبوع 2–3)
- **G5:** نموذج صفقة واحد (حذف/تحييد الشبكة العلوية)، حقل مورد متزامن يعرض الاسم لا `#id`. *تحقّق:* لا ازدواج حقول؛ snapshot اختبار واجهة.
- **G6:** تحقّق حقلي في نماذج الشحنة/الفاتورة (إبراز الحقل الناقص). *تحقّق:* حفظ شحنة ناقصة يبرز الحقل المحدّد.
- **G7:** بنود تنقّل «الصفقات» و«رحلة الاستيراد» في القائمة. *تحقّق:* الوصول دون بطاقة KPI.
- **G8:** N+1 وترقيم قوائم deals/shipments. *تحقّق:* عدّ استعلامات ثابت عند 2→6 صفوف.

### 🟡 M-P2 — الصقل والاحترافية (أسبوع 4+)
- **G9:** قاموس مصطلحات موحّد + إخفاء المعرّفات التقنية. *تحقّق:* grep لا يُظهر «مصنّع/مصنع» مختلطاً في الواجهة.
- **G10:** وصولية (aria/ tooltips/ تباين) + صيغة تاريخ محلية. *تحقّق:* فحص a11y آلي بلا أخطاء حرجة.
- **G11:** التسجيل غير الحاجب (بروتوكول 4) + إزالة `eval` + رؤوس أمان. *تحقّق:* لا `eval` في البناء؛ CSP مفعّل.
- **G12:** معالِج أول-صفقة + جولة تعريفية + حالات فارغة موجِّهة. *تحقّق:* مستخدم جديد يُكمل صفقة→شحنة دون مساعدة خارجية (اختبار قابلية استخدام).

---

## 7. المراجع (كود مؤكَّد)
- `logistics/serializers.py:268` — `LogisticsDealItemSerializer` (`product` إلزامي = جذر U1).
- `logistics/models.py:406,1202` — `product = ForeignKey(Product, PROTECT)`.
- `frontend_v2/App.tsx:494–1497` — مطابقة `/import-flow/` (جذر U3).
- `frontend_v2/components/forms/deal-parts/DealStageControl.tsx` — لوحة «الخطوة التالية».
- `frontend_v2/components/import-flow/ImportDocumentScreen.tsx` — رحلة الاستيراد 6-خطوات.
- `PROJECT_MAP.md` — أسرار defaults، حجم الحزمة، N+1، الهجرات 0052/0053، بروتوكول التسجيل المعلّق.
- `logistics/views.py` — `LogisticsShipmentViewSet` (بعد `get_queryset()`) — إضافة
  `_next_shipment_number`/`perform_create` (جذر U11، انظر §8.2 و`PROJECT_MAP.md` [LIVE TEST]).
- `frontend_v2/components/import-flow/ImportDocumentScreen.tsx:627–678` — `handleUpdateDealMeasure`
  (`PATCH logistics/deals/{id}/` بـ`total_cbm`/`total_weight_kg`) و`handleUpdateAllocation`
  (`PATCH logistics/shipments/{id}/` بـ`deal_allocations`) — آلية توزيع تكلفة الشحن (جذر U15).
- `logistics/views.py:934–962` — `LogisticsPaymentViewSet` (`logistics/payments/`) يخدم دفعات
  الصفقة **و**دفعات الشحنة للوكيل معاً (`shipment=X, deal=None`) — نقطة نهاية واحدة غير موثّقة
  بوضوح في الواجهة.

---

## 8. الجلسة الثانية (2026-07-18) — الإغلاق النهائي حتى فواتير دولية حقيقية

### 8.1 الملخّص التنفيذي
أُعيدت الجلسة بعد تفعيل الإصلاحات، ثم أُكملت الحالة المتوقفة D-0109، وأُنشئت صفقتان جديدتان
بموردين ومنتجين جديدين. جميع كتابات بيانات العمل في جولة الإغلاق (المورد، المنتج، الصفقة،
الدفعة، الشحنة، التوزيع، التخليص، والفاتورة) نُفّذت عبر الواجهة الفعلية.

| الصفقة | الشحنة | النتيجة |
|---|---|---|
| **D-0108** (Ningbo Ocean Power, $3,400) | SH-0001 | ✅ سجل الجلسة الأصلي: `INV-0002` بمبلغ ₪12,362. |
| **D-0109** (Chengdu Sunrise Electric, $3,000) | SH-0002 | ✅ أُكملت بعد التوقف السابق: `INV-0004` (id 3669) بمبلغ ₪13,670. |
| **D-0112** (Solar Harbor, $2,100) | SH-0015 | ✅ رحلة جديدة كاملة: `INV-0005` (id 3670) بمبلغ ₪8,990. |
| **D-0113** (Grid Motion, $2,400) | SH-0016 | ✅ رحلة جديدة كاملة: `INV-0006` (id 3671) بمبلغ ₪10,216. |

اختبار تحقق مستقل سجّل الدخول بالمستخدم الجديد وفتح الشحنات الثلاث، وشاهد في كل شاشة زر
«فتح الفاتورة» برقم الفاتورة الصحيح، كما أعادت واجهة الخادم السجلات نفسها بالقيم أعلاه.

### 8.2 U11 — عطل إنشاء الشحنة: الجذر والإغلاق
**العرض قبل الإصلاح:** `POST /api/logistics/shipments/` كان يفشل دائماً بـ
`400 {"shipment_number":["لا يمكن لهذا الحقل ان يكون فارغاً."]}` عند الحفظ من الواجهة.

**الجذر (مؤكَّد بالكود، مطابقة لنمط تشخيص فعلي عبر مراقبة `fetch` الحيّة):**
1. الواجهة: حقل «رقم الشحنة» في `ImportDocumentScreen.tsx` مضبوط `readOnly` ويفترض ترقيماً
   تلقائياً من الخادم (`#{id}` أو «— جديدة —») — فيرسل `shipment_number: ""` دائماً.
2. الخادم: `LogisticsShipmentViewSet` (`logistics/views.py`) **لا يملك** `perform_create`
   مخصّصاً — يرث فقط `TenantCreateMixin.perform_create` (يضبط `tenant` فقط)، بعكس
   `LogisticsDealViewSet` الذي يولّد `D-####` تلقائياً عبر `_next_deal_ref`.
   **النتيجة قبل الإصلاح: كان إنشاء شحنة عبر الواجهة مستحيلاً بنيوياً.**

**الإصلاح النهائي المتحقق:** أضيف توليد tenant-scoped بنمط `SH-####` في الخادم، والسماح
بالفراغ عند الإنشاء فقط، ومنع مسح رقم شحنة محفوظة عبر `PATCH`، مع Logging واختبارات عزل
الشركات. بعد إعادة تشغيل Django أُنشئت الشحنات الجديدة من الواجهة وحدها وتولّدت أرقامها
تلقائياً. نجحت اختبارات Django الكاملة وفحص انجراف الهجرات؛ انظر §8.6.

**حالة الـWorkaround القديم:** يبقى وصفاً تاريخياً للجولة الأولى فقط؛ لم يُستخدم لكتابات بيانات
العمل في جولة الإغلاق النهائية.

### 8.3 أعطال جديدة (U11–U16)

| # | الخطورة | الموضع | العَرَض المرصود | الإصلاح المقترح |
|---|---------|--------|------------------|------------------|
| U11 | 🔴 P0 | إنشاء الشحنة | Blocker كامل — انظر §8.2. `perform_create` مفقود + حقل الواجهة `readOnly` بلا اتصال فعلي بمصدر ترقيم. | تطبيق الإصلاح المذكور + اختبار e2e «إنشاء شحنة من الصفر عبر الواجهة» كثابت (regression guard) كي لا يتكرر غياب `perform_create` لموديل جديد مستقبلاً. |
| U12 | 🔴 P0 | فقدان بيانات | فشل حفظ الشحنة (400) يُصفّر **كل** الحقول المُدخَلة يدوياً (نوع الشحن، BL، الحاوية...) عند إعادة الرسم. مستخدم يملأ نموذجاً طويلاً ثم يخسره بالكامل لخطأ حقل واحد. | الاحتفاظ بحالة النموذج محلياً (`shipmentForm` state) بعد فشل الحفظ؛ عدم إعادة الضبط إلا بعد نجاح فعلي. |
| U13 | 🟠 P1 | مُنتقي المورد داخل نموذج الصفقة | مورد يُنشأ حديثاً («Chengdu Sunrise Electric») **لا يظهر في نتائج بحث** نفس النموذج بنفس الجلسة («لا توجد نتائج») رغم وجوده فعلياً (تأكّد عبر `/suppliers`, id #299). يتطلّب إعادة تحميل كاملة للصفحة ليصبح قابلاً للاختيار. | فحص طبقة الكاش/الفهرسة لمُنتقي الموردين — على الأرجح قائمة مُحمَّلة مرة واحدة عند mount ولا تُحدَّث بعد إنشاء عنصر جديد ضمن نفس الجلسة. إعادة جلب/دمج العنصر الجديد فوراً في نتائج البحث المحلية بعد نجاح الإنشاء. |
| U14 | 🟠 P1 | تأكيد دفعة المورد (سليب البنك) | مودال «تسجيل الدفعة» يفرض رفع صورة سليب تحويل بنكي قبل تفعيل زر التأكيد رغم أن الحقل (`bank_swift_image`) **غير إلزامي فعلياً** في الـserializer الخلفي (تأكّد بنجاح تأكيد دفعة $3,400 عبر API مباشر بلا صورة). فجوة بين قيد واجهة صارم وقيد خادم متساهل — تمنع مستخدماً بلا سليب جاهز فوراً من إكمال العمل رغم أن النظام يسمح تقنياً. | إمّا اعتبار السليب اختيارياً في الواجهة أيضاً (مطابقة الخادم)، أو توضيح صراحةً لماذا هو إلزامي واجهياً (ضبط سياسة عمل، لا مجرد قيد تقني منسي). |
| U15 | 🟠 P1 | توزيع تكلفة الشحن على الصفقات (خطوة 3 برحلة الاستيراد) | عندما تكون صفقة واحدة فقط بالشحنة وCBM/KG = 0 لبنودها، عمود «حصة الشحن (USD)» يبقى 0.00 بلا مسار واضح بالواجهة لإدخال CBM/KG يدوياً بثقة (الحقول قابلة للتعديل فعلياً — `handleUpdateDealMeasure` — لكن اكتشاف ذلك أثناء استخدام حيّ استغرق وقتاً طويلاً بسبب U16 أدناه). | تلميح واجهي صريح أعلى الجدول: «أدخل CBM أو KG أولاً» بدل الاعتماد على تلوين الحدّ الأحمر (`missing` state) فقط، خصوصاً حين الشحنة تضمّ صفقة واحدة (حالة شائعة لمستخدم جديد). |
| U16 | 🟡 P2 | استقرار واجهة رحلة الاستيراد | أثناء التفاعل المتكرر بخطوة توزيع الشحن، **موضع التمرير (scroll) يُعاد للأعلى بعد كل نقرة/تعديل حقل** — يجبر المستخدم على إعادة التمرير للأسفل بعد كل إجراء صغير، ويزيد احتمال النقر بالمكان الخطأ (نقرات ذهبت لعناصر أخرى غير مقصودة أثناء هذه الجلسة). يوحي بإعادة رسم (re-render) كامل للمكوّن عند أي تحديث حالة بدل تحديث جزئي. | تتبّع سبب إعادة الرسم الكامل (غالباً `key` غير مستقر أو state أعلى من اللازم يعيد mount للجدول)؛ الحفاظ على موضع التمرير عبر `scrollIntoView`/`ref` بعد التحديثات غير المرتبطة بالتنقّل. |

**الحالة النهائية:** أُغلقت U11–U16 كلها. U12 يحافظ على `shipmentForm` عند `400`؛ U13 يدمج
المورد الجديد ويختاره فوراً؛ U14 يعتمد السليب الاختياري بعقد واحد؛ U15 يعرض أسماء الصفقات
الناقصة؛ وU16 يحفظ موضع `main.app-content` ويستخدم صفاً `memo` بمفاتيح وحالة حفظ مستقرة.
الاختبار يثبت أيضاً أن الصف غير المتغير لا يُعاد تركيبه ويبقى مفعلاً.

### 8.4 قاعدة عمل مؤكَّدة (وليست عطلاً) — بوابة الفاتورة الدولية
القاعدة الخلفية صحيحة: لا تُنشأ فاتورة دولية قبل اكتمال دفع الصفقة بالدولار ووجود تكلفة تخليص
فعلية بالشيكل. نُفذت التوصية استباقياً: يعيد preview الحقول `deal_paid_usd`,
`deal_unpaid_usd`, و`deal_fully_paid`، وتعرض الواجهة الرصيد لكل صفقة وتعطّل الاستيراد قبل
الضغط. كُتب اختبار backend أولاً وفشل لغياب الحقول، ثم نجح بعد التنفيذ، وأضيفت اختبارات وحدة
لمنطق الجاهزية. تحققت القاعدة حياً في D-0109 وD-0112 وD-0113.

### 8.5 الأهداف G13–G17 — مغلقة

- **G13 ✅:** إنشاء شحنات من الواجهة يولّد `SH-####` تلقائياً؛ Django والهجرات أخضران.
- **G14 ✅:** بيانات نموذج الشحنة تبقى بعد خطأ الحفظ.
- **G15 ✅:** المورد الجديد يظهر ويُختار في الجلسة نفسها.
- **G16 ✅:** سليب البنك اختياري في الواجهة والخادم بعقد واحد واضح.
- **G17 ✅:** الصف المتغير فقط يدخل حالة الحفظ، والصف الآخر لا يُعاد تركيبه وموضع التمرير ثابت.
  استُخدم اختبار هوية DOM آلي قابل للتكرار بدلاً من الادعاء بقياس يدوي عبر React Profiler.

### 8.6 مصفوفة التحقق النهائية

- Django: **315/315** ناجحة مع `core.test_settings`، وSystem check بلا مشاكل.
- `makemigrations --check --dry-run --settings=core.test_settings`: **No changes**.
- Vitest: **32/32** ناجحة؛ TypeScript `--noEmit`: ناجح؛ Vite production build: ناجح.
- Playwright الموجّه للنقطة 8: **20/20** ناجحة، والرحلة الحية الكاملة والتحقق المستقل من
  الفواتير الثلاث ناجحان.
- تشغيل Playwright الكامل كشف **11 إخفاقاً قديماً خارج نطاق النقطة 8** (تهيئة مصادقة ناقصة،
  selectors قديمة، وسيناريوهات offline/request-budget). أُعيد تشغيل اختبارات النقطة 8 منفصلة
  وبقيت خضراء؛ لم تُوسّع الجراحة لتغيير سلوك غير مطلوب.
