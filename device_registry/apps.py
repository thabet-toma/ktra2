from django.apps import AppConfig


class DeviceRegistryConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "device_registry"
    verbose_name = "تسجيل وتتبع الأجهزة الحساسة"
