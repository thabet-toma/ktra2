# CHANGELOG — سجلّ التغييرات

> **سطر واحد لكل مهمة منتهية.** الأحدث أولاً.
>
> بديل خفيف لـ`docs/history/PROJECT_MAP.md` الذي بلغ **8,104 أسطر** لأنه لم يكن
> له سقف: كل مهمة كانت تُكتب فيه فقرةً كاملة، فصار قراءته أغلى من قراءة الكود —
> وهي «ضريبة السياق» التي بُنيت الخريطة الحالية لإلغائها.
>
> **القاعدة:** سطر واحد — التاريخ · الـcommit · ماذا ولماذا. التفاصيل تسكن في
> رسالة الـcommit نفسها (وهي مفصّلة في هذا المستودع)، لا هنا.

| التاريخ | Commit | التغيير |
|---|---|---|
| 2026-08-12 | — | **حُذف سكربت النشر** `deploy.ps1` + `deploy.cmd` بقرار المالك. كان الطريق الآلي الوحيد إلى `smart.ktragroup.com`، ومعه ذهب: ضبط gunicorn (‏P0-1: gthread وworkers×threads و`--timeout 180` وإعادة تحميل رشيقة بـSIGHUP) · تدقيق تسرّب الأسرار في الحزمة · النسخ الاحتياطي والاسترجاع التلقائي. كان يحمل أيضاً عطلاً غير مُصلَح: قائمة المجلدات اليدوية تشحن 10 apps من 13 (تنقصها `accountant_portal` و`after_sales` و`device_registry` — نفس الثلاثة التي سقطت من `testpaths`) فكان `manage.py check` ليُخفق على الخادم ويُشغّل الاسترجاع. يُسترجَع بـ`git checkout 72024cb -- deploy.ps1 deploy.cmd` |
| 2026-08-12 | — | **البوابة صارت مُلزِمة آلياً**: CI كانت تعمل على `main` وحده وهو متوقف منذ 2026-07-19 ⇒ صفر فحص آلي لـ`newktra` (فرع العمل المعتمد) — أُضيف للمشغّلات، وهو أيضاً ما يغطّي فارق Django ‏6.0.3 محلياً مقابل 5.1.15 إنتاجاً لأن CI تثبّت `requirements.txt`. وبوابة `deploy.ps1` قبل النشر كانت تشغّل `manage.py test` الناقص (يفوّت 253 اختباراً) فصارت `pytest -n auto` — أخطر المواضع لأنه يسبق الإنتاج مباشرةً |
| 2026-08-12 | — | **البوابة كانت تفوّت 18% من الاختبارات**: `manage.py test` (الأمر الموثَّق) يكتشف أصناف `TestCase` وحدها، و253 اختباراً هنا دوالٌّ بنمط pytest ⇒ لم تكن تعمل في البوابة المحلية أبداً. صار المُشغّل `pytest -n auto` (مجموعة فائقة مُثبَتة بفرق صفر في معرِّفات الاختبارات) بـ80ث، و`pytest-xdist` في المتطلبات. واختبار Ollama حيّ كان يحمّر البوابة لمن يضبط `OLLAMA_API_KEY` (الخطة المجانية ترد 403 على نموذج مدفوع) فصار يلزمه اشتراك صريح `OLLAMA_LIVE_TEST=1` |
| 2026-08-12 | — | **CI لم تكن تشغّل ثلاث apps**: `pytest.ini/testpaths` قائمة يدوية سقطت منها `accountant_portal` و`after_sales` و`device_registry` — 176 اختباراً (15% من المجموعة) خضراء محلياً وغائبة عن CI (وCI تشغّل pytest لا `manage.py test`). كانت موثّقة في `ARCHITECTURE.md` كحلٍّ التفافي «استخدم manage.py test» فحُلَّت من جذرها، وحارسٌ في `core/tests/test_docs_freshness.py` يمنع سقوط app جديدة |
| 2026-08-12 | — | **أربعة أخماس زمن الاختبارات كانت تهشيراً**: PBKDF2 بطيء عمداً (1.2 مليون تكرار = 1,287 مللي للتهشيرة) و300+ استدعاء `create_user` ⇒ المجموعة 773ث. `PASSWORD_HASHERS=MD5` في `core/test_settings.py` وحده (الإنتاج يبقى على PBKDF2) ⇒ 77ث تسلسلياً و21.6ث بـ`--parallel`. الغرض إزالة الإغراء بتخطّي البوابة أو تقسيمها بحسب الموديول — وهو تقسيمٌ غير آمن هنا لتشابك `accounting` (مثالان من جلسة واحدة) |
| 2026-08-12 | — | **قائمة فواتير الشراء 29.9ث ⇐ 500**: `Count('items')` تفرض GROUP BY فيمنع جانغو من تقليم التعليقات في استعلام العدّ، فصار العدّ `SELECT COUNT(*) FROM (…أربعة استعلامات دفع فرعية لكل صفّ…)` — 14.2ث للعدّ و15.7ث للصفحة على مستأجرٍ بـ1,261 فاتورة، فوق مهلة قراءة MySQL (30ث). صار العدّ استعلاماً فرعياً كـ`deals_count` في الشحنات، وحارسٌ يمنع رجوع GROUP BY إلى استعلام العدّ |
| 2026-08-12 | — | **توقيت العمل**: `TIME_ZONE` كان `UTC` والعمل في فلسطين ⇒ مستند يُنشأ بين منتصف الليل والثالثة فجراً يُؤرَّخ **بيوم أمس** (خطأ تأريخ محاسبي صامت). صار `Asia/Hebron`، ووُحِّد مصدر «اليوم» على `timezone.localdate()` في **61 موضعاً/26 ملفاً** (كان مختلطاً مع `date.today()` = تاريخ نظام الخادم)، وحارس يمنع الرجعة. كشفه إخفاق `test_dashboard_isolation` الذي لا يقع إلا بين 00:00 و03:00 |
| 2026-08-12 | — | `tenants/__init__.py` لم يكن موجوداً أصلاً — الـapp كانت تعمل كـnamespace package وحدها من بين 13، وهو خطر دمج مع نسخ المستودع في `.claude/worktrees/` |
| 2026-08-12 | — | **خريطة تحرس حالها**: حارس طزاجة (`core/tests/test_docs_freshness.py`) + مولِّد (`sync_docs`) + `docs/API_INDEX.md` (692 نقطة) + وثيقتا `core` و`frontend` + سياسة «الرموز لا الأسطر» (189 رقم سطر حُذف). `AGENTS.md` كان يناقض الواقع فاختُصر، وهوك ميت أُزيل |
| 2026-08-11 | `08afc89` | كاش الملفات كان يُسقط الطلب 500 عند تعثّر نظام الملفات — `_delete` في Django يلتقط `FileNotFoundError` وحدها، وعلى ويندوز الحذف المتزامن يرمي `PermissionError`. التطوير صار `LocMemCache` والإنتاج `ResilientFileBasedCache` |
| 2026-08-11 | `4fd45c1` | خمس نتائج مراجعة الجلسة: بانر قصّ كشف الأستاذ (تعليق كان يدّعي سلوك واجهة غير موجود) · فهرسا `SupplierQuotation`/`PurchaseOrder` · عدّاد أجيال لنوافذ الكاش في الواجهة · تخفيف P2-14 · أمر `audit_fk_orphans` |
| 2026-08-11 | `fcb5c19`→`af57dbf` | جلسة «كل النواقص»: إغلاق **P0-3** (آخر ثغرة P0 — سجلات الحضور كانت مقروءة عبر الشركات) + كل بنود P1 القابلة للتنفيذ + 11 من 14 في P2 + عدّة اختبار الحمل (المرحلة 6) |
