# أرشيف برنامج «الأصيل» (Al-Aseel 2005) — **تاريخيّ، ليس مرجع تصميم**

> **أُسقط كمرجعية بقرار المالك 2026-08-25.** هذا المجلد **أرشيفٌ صامت**: يشرح
> *لماذا* اتُّخذت قرارات قديمة في KTRA، ولا يُستشار إلا بطلب صريح. **ممنوع
> الاستشهاد به** لتبرير شكل شاشة جديدة أو سلوكها؛ مراجع التصميم المعتمدة اليوم
> هي دفترة · Odoo · Zoho Books · QuickBooks · Xero وما هو أفضل منها في السوق.
>
> المظهر الكلاسيكي في المنتَج **يبقى خياراً كاملاً** في الإعدادات — إسقاط
> المرجعية شيء، وحذف المظهر شيء آخر لم يُطلب.

نصوص مستخرَجة من مانوال برنامج الأصيل، كانت تُستخدم مصدرَ حقيقة (spec) لإعادة
بناء شاشات الإدخال في KTRA ERP بنمط الأصيل (راجع `task4.md`).

## المصدر

ملفات Word/RTF (CP1256، صور مضمّنة) في:
`C:\Castle\Asseal2005\Book\*.doc`

## إعادة التوليد

```bash
python tools/rtf_extract.py "C:/Castle/Asseal2005/Book/<اسم الملف>.doc" "docs/aseel_reference/<out>.txt"
```

`tools/rtf_extract.py` يفك RTF: يحوّل `\'xx` (CP1256) و`\uN`، يتخطّى الصور
وجداول الخطوط/الأنماط، ويُخرج نصاً UTF-8 نظيفاً.

## الملفات

| الملف | الفصل في الأصيل | يخدم |
|------|------------------|------|
| `intro.txt` | المقدمة (مزايا/أقسام النظام) | نظرة عامة، تكاليف الاستيراد، تعدد العملات/الدفاتر |
| `invoices.txt` | الفواتير (بيع/شراء/مرجع) | M1, M2, M4 — شاشة الفاتورة + الحقول + المنطق |
| `accounting.txt` | المحاسبة (حسابات/قيود) | M4 — القيد اليدوي + شجرة الحسابات |
| `inventory.txt` | المخازن (الأصناف) | حقول الصنف + الوحدات + الأسعار |
| `shipments.txt` | الإرساليات + البيان الجمركي | M3 — الشحنة/التخليص/النقل المحلي |
| `quotations.txt` | العروض والطلبيات | M4 — العروض/الطلبيات |
| `notices.txt` | الإشعارات (مدينة/دائنة) | M4 — الإشعارات |
| `cheques.txt` | الشيكات | M2 — السند المرفق + الشيكات |
| `financial.txt` | المعاملات المالية (سند قبض/صرف) | M4 — سند القبض/الصرف |

> كل تاسك في `task4.md` يذكر المرساة (الملف + رقم السطر). الاستخراج آلي وقد
> يحوي بقايا رموز أشكال (`shapeType…`) — تُتجاهَل؛ النص العربي هو المرجع.
