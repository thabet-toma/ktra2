# frontend — واجهة React (`frontend_v2/`)

## الغرض

الـSPA كاملةً: React 19 + TypeScript 5.8 + Vite 6 + Tailwind 4. تتحدّث مع Django
عبر `/api/` حصراً.

**الملفات مباشرةً تحت `frontend_v2/` — لا مجلد `src/`.**

## أهم الملفات

| المسار | الغرض |
|---|---|
| `frontend_v2/App.tsx` | الجذر: التوجيه، 97 صفحة lazy، تركيب السياقات |
| `frontend_v2/services/restApi.ts` | **الـbase client** — مهلة 30ث، إعادة محاولة GET، ترويسات الشركة/الفرع |
| `frontend_v2/services/` | 52 ملفاً — خدمة لكل دومين (`salesApi`, `accountingApi`, `inventoryApi`, `dealsService`…) |
| `frontend_v2/components/kit/` | **عُدّة الواجهة المشتركة**: `KitDocumentShell`, `KitDenseTable`, `KitDocumentView`, `KitAutocomplete`, `KitFloatWindow` |
| `frontend_v2/utils/windowGeometry.ts` · `frontend_v2/hooks/useDragResize.ts` | **هندسة النوافذ العائمة**: القصّ والسحب والتحجيم والحفظ (`ktra:win:<الاسم>`) + جهة إرساء الشريط |
| `frontend_v2/components/layout/ActionBarRail.tsx` | **شريط الإجراءات القابل للإرساء** — يمين/يسار رفّاً عائماً، أو أفقياً في شريط العنوان |
| `frontend_v2/components/layout/QuickAccessBar.tsx` · `frontend_v2/utils/quickBarPref.ts` | **شريط الوصول السريع** (رجوع + المسار + الاختصارات + مرسى المرشد) — يُطوى بضغطة أو `Ctrl+F1`، والتفضيل لكل مستخدم |
| `frontend_v2/components/office/ClientBooksPanel.tsx` · `frontend_v2/utils/managedBooks.ts` · `frontend_v2/services/managedBooksApi.ts` | **دفاتر عملاء المكتب (ISSUE #65)**: اللوحة الواحدة تُركَّب في القشرتين (قسم «دفاتر عملائي» في `/office` وشاشة `client-books` في قشرة الشركة)، و`pickOfficeTenant` تحسم الشركة التي تلعب دور المكتب. قالبها **الوحيد** `client_book` (ISSUE #81 ثم بلاغ المالك، `DEFAULT_CLIENT_BOOK_TEMPLATE` في `utils/companyTemplates.ts`): كان منتقياً بثلاثة خيارات يفتح خياراه الآخران نظاماً تجارياً كاملاً للزبون، فصار سطراً يقول القالب لا `select` — وكذلك في `components/accountant/office/OfficeExternalClientPage.tsx` التي كانت تفتح افتراضياً على `general`. الخادم يفرض القاعدة نفسها (`assert_book_template`، 400) فالمنتقي عرضٌ لا حارس. ومقابلها `SELF_SERVE_COMPANY_TEMPLATES` في أبواب إنشاء الشركة الثلاثة (`onboarding/FirstCompanyOnboarding.tsx` · `layout/CompanySwitcher.tsx` · `layout/CompanyManagementModal.tsx`) — بلا `client_book` |
| `frontend_v2/contexts/` | 9 سياقات: `Auth`, `Company`, `Permissions`, `Appearance`, `SessionSettings`, `Theme`, `Toast`, `Confirm`, `PriceVisibility` |
| `frontend_v2/utils/navAccess.ts` | اشتقاق القائمة: الصلاحية + قناع «الوضع السهل» — **نقطة التركيب الوحيدة** |
| `frontend_v2/utils/uiMode.ts` | «الوضع السهل»: `SIMPLE_VIEWS` وقناع العناصر (`SIMPLE_MASK`, `SIMPLE_HIDDEN_COLUMNS`, `showAdvanced`, `visibleColumns`) وcache بمفتاح الشركة |
| `frontend_v2/hooks/useSimpleUi.ts` | **غلاف قناع العناصر الوحيد** — `show()` للحقول والأزرار والتبويبات، `columns()` لأعمدة الجداول |
| `frontend_v2/components/ui/FieldHint.tsx` · `frontend_v2/constants/simpleHints.ts` | أيقونة «؟» ونصوصها — **كل النصوص في الملف الثاني وحده** |
| `frontend_v2/components/accounting/AccountTreePicker.tsx` | **اختيار الحساب في كل الشاشات**: `AccountTreePicker` (النافذة) و`AccountTreeField` (بديل `<select>`) |
| `frontend_v2/components/accounting/DocumentCodingPage.tsx` | **ISSUE #85 — الترميز الدفعي**: شبكةٌ واحدة كثيفة (`KitGrid`) لا نمط عمودين، فوق `POST vouchers/batch-save/` و`coding-rules/` اللتين بنتهما #84؛ الحساب يُقترح من الطرف فور كتابته (قابلٌ للتجاوز بالكتابة فوقه بلا سؤال)، و«ناقِش» على الصفّ يفتح `ReviewQuery` القائم (`accountantApi.createReviewQuery`) لا محادثة جديدة. **متابعة #85**: عمود «طريقة الدفع» (نقد افتراضاً/على الحساب) — `frontend_v2/utils/codingRowPaymentMethod.ts` (`paymentFieldsForRow`) يرفق صفّ النقد بصندوق الشركة الافتراضي (`utils/cashBox.ts`) إن عُرف، ويترك الحقل للخادم إن غاب |
| `frontend_v2/utils/accountTree.ts` | منطق الشجرة: الفهرس، البحث، القصّ بالغرض (`accountMatchesPurpose`, `buildAccountSelectable`) |
| `frontend_v2/components/ui/FileDropZone.tsx` | **منطقة رفع الصور الموحّدة**: اختيار + سحب وإفلات + لصق — لا `<input type="file">` للصور خارجها |
| `frontend_v2/utils/clipboardImage.ts` | سجلّ مناطق اللصق ومُوجِّهه (`resolvePasteZone`, `usePasteZone`, `usePasteImageUpload`) |
| `frontend_v2/utils/formatNumber.ts` | **كل عرض رقمي يمرّ من هنا** |
| `frontend_v2/utils/terms.ts` | **ISSUE #82 — المعجم**: `resolveTerm` (نقطة القراءة الصرفة) — مصدر الحقيقة `terms` في حمولة `/api/permissions/me/`؛ `usePermissions().term(key)` الغلاف التفاعلي |
| `frontend_v2/components/accounting/RevenueVouchersPage.tsx` · `frontend_v2/utils/revenueVoucherEntryPreview.ts` | **سندات الإيراد (issue #80 خادمياً، والشاشة من بلاغ المالك)**: النقاط الثلاث (`revenue-vouchers/` قائمةً وإنشاءً و`unpost/`) بُنيت في الخادم منذ #80 وبقيت **بلا مستدعٍ واحد** — فكان سند الإيراد يُكتب من شاشة الترميز الدفعي وحدها ثم لا يظهر في أي قائمة ولا يُتراجَع عنه إلا من دفتر اليومية. الشاشة مرآةٌ متعمَّدة لـ`ExpenseVouchersPage`، ومعاينة القيد **مقلوبة الاتجاه** (الصندوق مدين وحساب الإيراد دائن) — يحرسها `revenueVoucherEntryPreview.test.ts` |
| `frontend_v2/utils/codingSavedVouchers.ts` | **سِجلُّ ما حُفظ في جلسة الترميز (بلاغ المالك: «ما ببيّنو بنفس الصفحة»)**: `handleSave` يمسح الصفوف الناجحة من الشبكة ويكتفي بإشعارٍ عابر، فلا أثر على الشاشة لما حُفظ — لا رقمُ سند، ولا تحقّقٌ من أن الحساب المُقترَح هو المقصود، ولا تراجعٌ إلا بمغادرة الشاشة. **مسحُ الصفّ الناجح يبقى** (الشبكة قائمةُ عملٍ لِما لم يُحفظ، وبقاؤه فيها يدعو لحفظه مرّتين)، والسِجلّ يجاورها. `buildSavedVouchers` تأخذ الاتجاه من الردّ **وإلا من الصفّ المُرسَل** — `direction` اختياريّ في `VoucherBatchSaveRowResult` والاتكال عليه وحده يعني استدعاء نقطة المصروف على سند إيراد. السطرُ المتراجَع عنه يُشطب ولا يُحذف: اختفاؤه يُنسي المستخدمَ فعلَه فيعيد ترميزه |
| `frontend_v2/utils/voucherAccountEntryMode.ts` | مرآة `TenantSettings.voucher_account_entry_mode` — نقطةُ قراءةٍ واحدة تستهلكها شاشتا السندين لإخفاء خانة «أو اسم جديد» في وضع `linked`. تفشل **مفتوحةً** (غياب القيمة = `free`) عكسَ `moduleAllowsView`: الحارس الحقيقي خادميّ وهذا إخفاءُ حقل |
| `frontend_v2/utils/shellManifest.ts` | **ISSUE #83 — بيان الشريط**: نقطة القراءة الصرفة فوق `shell` في حمولة `/api/permissions/me/` — `filterShellGroups`/`resolveManifestView` تُقصيان ما يرفضه القناع الحيّ (`viewPermissions.ts`) أو ترخيص وحدة أو شاشةً لم تُبنَ بعد (`unbuilt_views`)؛ `buildShellSections` تضيف فوقها الصلاحية (`visibleLinks`/`permForView`) فتُعيد أقسام الشريط الجاهزة للرسم — دالّةٌ صرفة بلا React يستهلكها `Sidebar.tsx`؛ `resolveShellFirstAction` يخدم شريط الإجراءات وقائمة الفأرة اليمنى. `null` لـ`general` — لا تغيير |
| `frontend_v2/utils/formatDate.ts` | التواريخ — لا `toLocaleDateString` بلغة عربية |
| `frontend_v2/services/tenantSettingsApi.ts` | مصدر مشترك لإعدادات الشركة (نافذة 60ث) |
| `frontend_v2/components/legacy/firestoreService.ts` | الجسر القديم — **يُستورَد ديناميكياً** لا ثابتاً |

## بنية المكوّنات

39 مجلداً فرعياً تحت `components/`، أهمّها بحسب الدومين: `sales/` · `accounting/` ·
`inventory/` · `logistics/` · `import-flow/` · `partners/` · `procurement/` ·
`finance/` · `hr/` · `reports/` · `settings/` · `superadmin/`.
والمشترك: `kit/` (عُدّة الواجهة) · `common/` · `shared/` · `ui/` · `layout/`.

**ISSUE #87 — شاشتا البداية للقالبين**: `App.tsx` (حالة `dashboard`) تحسم
بـ`utils/homeScreen.ts` (`resolveHomeScreen(template)` — دالّةٌ صرفة، سبق فرع
المدير/الموظف) أيّ شاشةٍ تُرسَم بدل `Dashboard.tsx` العامة، قبل بناء
`core/shell_manifest.py` (ISSUE #83: `start_view: "dashboard"` لكلا القالبين —
مفتاح الشاشة نفسه، والمحتوى وحده يتغيّر): `accounting_firm` ⇒
`components/office/OfficeHomeDashboard.tsx` — ثلاثة عناصر لا رابع (القرار 24
في #46: زبائن المكتب وحالة دفتر كلٍّ منهم · استحقاقات قريبة · أتعاب غير
محصّلة)، يستهلك `practice/dashboard/` (ISSUE #58) كما هي بلا شاشة تنقّلٍ بين
شركات (تلك تبقى في `AccountantOfficeApp`/`OfficeDashboardPage` على `/office`).
`client_book` ⇒ `components/office/ClientBookFinancialPosition.tsx` — إيراد ·
مصروف · ربح · ضريبة صافية للفترة، مع اتجاه ستة أشهر، يستهلك
`ClientSummaryView`/`ClientTrendView` (`accountant_portal`) حرفياً بلا نقطة
جديدة؛ الضريبة الصافية من `client_financial_summary` وحدها (issue #79،
`vat_period_totals`). `general` بلا تغيير. يحرسه `utils/homeScreen.test.ts`.
**مراجعة**: `client_book` صار يُرخَّص لوحدة `accountant_portal` تلقائياً عند
الزرع (`tenants/services.py` — `create_company`، `docs/modules/tenants.md`)،
لكن دفاتر **قائمة** أُنشئت قبل هذا الإصلاح تبقى بلا ترخيص. `ClientBookFinancialPosition.tsx`
يفحص `usePermissions().modules.accountant_portal` استباقياً عبر
`utils/homeScreen.ts` (`resolveModuleGate` — دالّةٌ صرفة) **قبل** أي نداء شبكة:
تحميل الصلاحيات ⇒ هيكل عظمي، `false`/غياب المفتاح ⇒ رسالة عربية صريحة تقول
الناقص وكيف يُفتح (لا 404 خام ولا شاشة بيضاء)، `true` صراحةً ⇒ الشاشة كما هي.

**T-SCAN — التعرّف على رقم**: زرّ البحث في الترويسة (`layout/GlobalSearch.tsx`)
كان روابط سريعة بلا حقل بحثٍ واحد؛ صار يفتح `shared/ScanLookupPanel.tsx` — حقلٌ
واحد يقبل الباركود والسيريال والـIMEI ورمز المنتج وجزءَ الاسم، وزرّ كاميرا
(`shared/BarcodeScannerModal.tsx`) بجانبه. اللوحة **لا تستنتج** نوع الرقم ولا
تفحص صلاحية: `kind` و`scope` وترتيبُ المطابقات تصل محسوبةً من `/api/scan/`.
والروابط السريعة صارت **حالةَ الفراغ** داخلها. والكاميرا تُعطَّل على اتصالٍ غير
آمن ويُكتب السبب **قبل** الضغط (`window.isSecureContext`) — قيدُ متصفحٍ على
`getUserMedia` لا خيارٌ لنا. يحرسها `e2e/scan-lookup.spec.ts` (بما فيه أن
البطاقة كلّها تصل بنداءٍ **واحد**، وأن الزرّ مُعطَّل ومشروحٌ على غير HTTPS).

**العرض الشجري للمنتجات = شجرة + بطاقة**: `items/ItemsManagement.tsx` كان يرسم
شجرة المنتجات (`procurement/invoices/InvoiceCategoryTree.tsx` — شريط 240px
تفرضه `.ktra-tree-panel`) ويترك بقية عرض الشاشة بياضاً، والنقر على منتج أو
تصنيف يفتح كرته في **تبويب جديد**. صارت المساحة بطاقةَ ما هو محدَّد في الشجرة:
منتجٌ ⇒ `ProductTreePane` (فوق `useProductInsights`) · تصنيفٌ ⇒ `GroupTreePane`
(فوق `useGroupInsights`) — والخطّافان في
`frontend_v2/components/items/ProductInsightTabs.tsx` يخدمان البطاقة الجانبية
والصفحتين الكاملتين (`ProductProfilePage` · `GroupProfilePage`) معاً، فلا نسخة
ثانية من تبويبات الكرت. النمط من شجرة الحسابات
(`frontend_v2/components/accounting/AccountingCoaPage.tsx`). الكرت الكامل
والتحرير يبقيان زرّين في رأس البطاقة.

**كبسةُ التصنيف تختار وتكشف**: حيث تُمرَّر `onShowGroup` (شاشة المنتجات وحدها)
تعرض الكبسة الكرتَ المجمّع **وتفتح الفرع** إن كان مطويّاً — ولا تطويه بإعادة
الكبس (الطيّ مهمّة السهم). وطيُّ اللوحة صار `.ktra-tree-rail` (32px بعنوان
عمودي) بدل `.ktra-tree-panel w-10`: الأخيرة تخسر أمام `flex: 0 0 240px` في
`styles/index.css` — القاعدة خارج طبقات Tailwind — فكان «الطيّ» يترك 240px
بيضاء ولا يعيد للشاشة شيئاً. يحرس هذا كلَّه `e2e/items-tree-card-pane.spec.ts`.

## العقود مع الخادم

| العقد | القاعدة |
|---|---|
| الشركة | كل طلب يحمل `X-Tenant-Id` (و`X-Branch-Id` عند اللزوم) — يضيفها `restApi.ts` |
| الترقيم | نقاط «الفئة أ» **ترقيمها إلزامي** — مرّر `?page=` وإلا قد يُردّ الطلب. الـautocomplete يبقى بلا ترقيم |
| المحدِّدات | منتجات المستندات عبر `listPickerProducts` — `GET /api/lookup/products/` (ISSUE #88؛ خارج `/api/inventory/` كي لا يبتلعه قناع قالب المكتب) لا العقد الكامل |
| الصلاحيات | `/api/permissions/me/` **للعرض فقط** — إخفاء زر ليس حماية |
| الوحدات المرخّصة | غير المفعّلة ترد **404** لا 403 — عالِج الحالتين |
| وضع العرض | `ui_mode` يصل ضمن حمولة `/api/permissions/me/` نفسها (بلا طلب إضافي)، ويُحفَظ بـ`POST /api/tenants/companies/set-ui-mode/` |
| المعجم (ISSUE #82) | `terms` يصل ضمن حمولة `/api/permissions/me/` نفسها — لا تكتب اسم مصطلحٍ حرفياً في شاشة، اقرأه بـ`term(key)` (`usePermissions()`، `utils/terms.ts`) |
| بيان الشريط (ISSUE #83) | `shell` يصل ضمن حمولة `/api/permissions/me/` نفسها — `null` لـ`general`. عرضٌ لا تصريح: أي مستهلكٍ يمرّ شاشاته عبر `filterShellGroups`/`resolveManifestView` (`utils/shellManifest.ts`) قبل الرسم أو التنقّل — لا رسم شاشةٍ يرفضها القناع الحيّ لمجرّد ذكر البيان لها |

## «الوضع السهل» — قناعُ عرضٍ فوق نفس الواجهة

نسخة مبسّطة للتاجر المبتدئ: **قناع لا منتج ثانٍ ولا واجهة ثانية**. لا شاشة جديدة بُنيت،
ولا سطر خادميّ محاسبيّ تغيَّر.

- **عرضٌ لا صلاحية.** يقلّم ما يُعرَض أولاً ولا يحجب مساراً: الرابط المباشر لأي شاشة
  متقدمة يبقى يعمل في الوضع السهل («مخفيّ لا محذوف»)، والحارس الوحيد يبقى الصلاحيات.
  **الصلاحية تحجب والوضع يُرتّب** — والوضع لا يمنح أبداً ما منعته الصلاحية.
- **نقطة التركيب واحدة**: `utils/navAccess.ts` (`linkVisible`) بوسيط `uiMode` اختياري،
  فوق قائمة الشاشات في `utils/uiMode.ts` (`SIMPLE_VIEWS`). حذف الوسيط = السلوك القديم
  حرفياً. **لا تُضِف آلية إخفاء ثانية** فوق القائمة — آليتان متنازعتان هما كيف تموت شاشة
  بصمت. `App.tsx` (التوجيه) لا يُمَسّ.
- **مصدر الحقيقة الخادم**، والـcache المحلي (`ktra_ui_mode::<tenantId>`) لتطبيقٍ فوري بلا
  وميض قبل رد `/permissions/me/`. أي قيمة غير معروفة — خادم أقدم، cache فاسد، حمولة ناقصة
  — تُطبَّع إلى `advanced` (`utils/uiMode.ts` — `normalizeUiMode`). الافتراضي هو التجربة
  الكاملة؛ التبسيط اختيارٌ صريح.
- **الحفظ تفاؤلي ولا يرتدّ تحت يد المستخدم**: الحالة والـcache فوراً ثم POST في الخلفية،
  وفشلُه يُبقي الوضع عاملاً في هذا المتصفح مع toast واحد بنبرة «معلومة» أن الحفظ عبر
  الأجهزة لم يتم — وهو مسار دور `viewer` المعروف (ممنوع من كل كتابة على المنصة).
- **زر التبديل ظاهر في الوضعين** في ذيل الشريط الجانبي (بجوار مبدّل «وضع المحاسب» الذي
  يختفي في السهل): طريق العودة يجب ألا يمرّ بشاشةٍ يخفيها القناع نفسه.
- **قاعدة السقوط للظهور — «يظهر رغم الوضع»**: عنصرٌ يشرح افتراضاً **مطلوباً لم يُحَلّ**
  يبقى ظاهراً في الوضع السهل. في `SalesInvoiceEditor.tsx` (`unresolvedRequiredDefault`):
  محدّد العملة يظهر ما لم تحسمه الإعدادات فعلاً، وتبويب «بيانات أخرى» يبقى ما دام تحذير
  إعدادٍ حيّاً. لا فشل صامت ولا حالة بلا مخرج — وهذه القاعدة فوق أي قائمة إخفاء.
- **العنصر المخفيّ يحتفظ بقيمته وحالته**، فحمولة الوضع السهل مطابقة لحمولة المتقدّم لمن
  ترك الافتراضيات ⇒ **نفس القيد المحاسبي بالضبط**. يسمّره خادمياً
  `sales/tests/test_simple_mode_journal_parity.py`.
- **طبقة «؟»**: `FieldHint` يقرأ الوضع بنفسه ويختفي في المتقدّم (فلا يُنسى الشرط في موضع
  نداءٍ من ثمانية عشر)، ولا يحمل نصاً — كل حرف في `constants/simpleHints.ts`، ومفتاحٌ
  بلا نصّ يعني لا أيقونة لا انكسار.
- **والقناع قناعان** (T-SIMPL2): بعد قائمة الشاشات، قناعٌ ثانٍ للعناصر **داخلها** — سِجلُّه
  `SIMPLE_MASK` في `utils/uiMode.ts` (مفتاحٌ لكل مفهوم ومعه سببُ طيّه مكتوباً)، وأعمدةُ الجداول
  في `SIMPLE_HIDDEN_COLUMNS` بمفتاح الشاشة (اسم `AppView` نفسه). **المفاتيح مفاهيمُ لا مواضع**:
  `doc.tax` واحدةٌ لفاتورة البيع وفاتورة الشراء معاً فلا يفترق الوضعان على شاشتين.
  المطويّ اليوم: تاريخ الاستحقاق · الضريبة (النسبة والعمود وسطور المجاميع وتحذير إعدادها) ·
  خصم السطر · العملة حين تحسمها الإعدادات · رقم المشتغل المرخّص · التبويبات المتقدّمة ·
  شريط أثر التشخيص · فلاتر النوع/الفئة · التعيين الجماعي للنوع · تحليلات المهام في الرئيسية ·
  إدارة التخزين المحلي في الإعدادات · وأعمدةٌ في القوائم الخمس.
- **الغلاف التفاعلي واحد**: `hooks/useSimpleUi.ts` — `show(key, keepIfSet?)` و`columns(cols, screen, keep?)`.
  **لا تكتب `uiMode === 'simple'` في شاشة**، ولا تُضِف مكوّن غلافٍ ثانياً فوقه: آليتان
  متنازعتان فوق عنصرٍ واحد هما كيف يختفي حقلٌ بصمت. الشاشة تبني قائمتها كاملةً (`allColumns`)
  ثم تُمرّرها من القناع — فالوضع المتقدّم يبقى مطابقاً حرفياً لما كان.
- **قاعدة السقوط للظهور على مستوى العنصر** (`keepIfSet` / `keep`): ضريبةٌ محسوبة، استحقاقٌ
  مُدخَل، خصمُ سطرٍ غير صفري، رصيدٌ محجوز، حدُّ ائتمانٍ مضبوط، فلترٌ مفعَّل، رسومٌ بمبلغ —
  كلّها تُعيد عنصرها رغم الوضع السهل. **الإخفاء يقلّم الصفر ولا يُخفي رقماً يغيّر مالاً**،
  ولا يترك قائمةً مُرشَّحة بفلترٍ لا يراه صاحبها. هنا نتجاوز المرجع: في Odoo إطفاءُ مجموعة
  (`group_discount_per_so_line`) يُخفي الحقل ولو كانت قيمته غير صفرية.
- **على القوائم المرقَّمة تُثبَّت حقيقة `keep`** بـ`useKeepOnce`: صفحةٌ واحدة ليست الجدول كلّه،
  وبلا تثبيت يظهر العمود في صفحة ويختفي في التي تليها. العمود **يظهر ولا يعود يختفي**.
- **ولا سطر خادميّ واحد**: العنصر المخفيّ يحتفظ بقيمته، فحمولة الوضعين واحدة ⇒ نفس القيد.
  الدوال الصرفة يحرسها `frontend_v2/utils/uiMode.test.ts`، ومفاتيحُ الأعمدة حارسٌ **نصّي على
  المصدر** `frontend_v2/utils/simpleMaskColumns.test.ts` (مفتاحٌ مطويّ بلا عمودٍ يقابله، أو شاشةٌ
  مسجَّلة لا تستدعي القناع — كلاهما يمرّ صامتاً بلا هذا الحارس)، والرحلة `e2e/simple-ui-mode.spec.ts`.
- **ترتيب المزوّدات مقصود**: `ToastProvider` **فوق** `PermissionsProvider` في
  `frontend_v2/index.tsx` — الأخير يحتاج `useToast` (وهو يرمي بلا مزوّد). لا تُعِد الترتيب.
- الرحلة كاملةً يحرسها `frontend_v2/e2e/simple-ui-mode.spec.ts`.

## اختيار الحساب — الغرض لا شرطٌ لكل شاشة

أي حقل حساب يستعمل `AccountTreeField` (أو `AccountTreePicker` مباشرةً)، ويصرّح
بغرضه بدل أن يكتب شرطه بنفسه: `purpose="cash" | "bank" | "receivable" |
"payable" | "inventory"` تُقرأ من التصنيف المخزَّن `Account.sub_type` الذي
يشتقّه الخادم (`accounting/account_classification.py`)، و`"asset" | "liability"
| "equity" | "revenue" | "expense"` من `account_type`، و`"any"` تقبل الكل.
الحقل المركّب يمرّر مصفوفة (`purpose={['expense','asset']}`).

- **الشجرة تُقصّ لا تُعتّم**: يُعرض القابل للاختيار **وآباؤه** فقط (الآباء
  رماديون للتوجيه)، وما ليس مطابقاً ولا أباً لمطابق لا يُرسم — المخزون لا يظهر
  في حقل الصندوق.
- **الافتراضي مع الغرض**: مطابقٌ **ونشط وورقة**. `allowParents` ترفع شرط الورقة
  وحده (الغرض يبقى) وتأخذها فئتان: فلاتر التقارير ودفتر الأستاذ لأنها تختار
  نطاق عرض لا هدف ترحيل، **وحقول الحسابات الافتراضية في شاشات الإعدادات** لأنها
  حسابات تحكّم يرحّل عليها النظام أصلاً — `sales/services/foundation.py` يبذر
  `1103` في «ذمم العملاء» وهو حساب أب في كل الشركات، فقاعدة «ورقة فقط» كانت
  تعرض المحفوظ ولا تسمح باختياره ثانيةً. التضييق يبقى على منتقيات سطور الحركة.
- **لا طريق مسدود**: صفر حساب مصنَّف لهذا الغرض ⇒ تُعرض الشجرة كاملة **ويسقط
  الغرض عن الانتقاء أيضاً** (`accountTree.ts` (`resolveAccountSelectable`)) —
  إسقاطه عن الرسم وحده كان يترك المستخدم يرى كل حساباته ولا يستطيع اختيار واحد.
  وشرط الشاشة الخادمي لا يسقط أبداً. وزر «عرض الكل» متاح دوماً للخروج من القصّ.
- `isSelectable` تبقى للشرط الخاص بالشاشة (قائمة يحدّدها الخادم) وتُركَّب فوق
  الغرض؛ وحدها كافية لتفعيل القصّ.

**من يصرّح بماذا** (لا شرط حساب محلي باقٍ في أي شاشة):

| الشاشة | الغرض |
|---|---|
| `GroupConstantsPage` · `SalesSettingsPage` | `revenue` · `cash` · `receivable` · `inventory` · `expense` · `liability` حسب الحقل — **كلها `allowParents`** (حسابات تحكّم) |
| `PurchaseSettingsPage` | `cash` + `allowParents` |
| `SalesOrdersPage` · `SalesCustomerPaymentsPage` · `NewSupplierPaymentModal` · `PurchaseInvoiceAccountingPanel` | `cash` (ورقة نشطة) |
| `PurchaseInvoiceAccountingPanel` (حساب البند والرسم) · `InvoiceForm` (حساب الرسم) | `['expense','asset']` |
| `ReportRunnerPage` | `cash` لفلتر `cash_account`، و`any` + `allowParents` لبقية فلاتر الحساب |
| `AccountingGeneralLedgerPage` | `any` + `allowParents` |
| `AccountingJournalEntryPage` (حساب السطر) | `any` — ورقة نشطة، فلا يُرحَّل على حساب عنوان |
| `SettleFromOnAccountModal` | لا غرض: `isSelectable` من قائمة الخادم، ويكسب القصّ بها |
| `SalesInvoiceEditor` (صندوق لوحة التحصيل) | لا غرض: `isSelectable` من صناديق الشاشة نفسها (`accountMatchesPurpose(a, "cash")`) |

`SalesInvoiceEditor` يفحص صناديقه بـ`accountMatchesPurpose(a, "cash")` — لا نسخة
ثانية من الشرط — ويمرّرها إلى المنتقي في لوحة التحصيل، وإلى قائمته المسطّحة في
بقية حقوله.

**تصحيح تصنيف حساب**: بطاقة الحساب في `AccountingCoaPage.tsx` فيها «التصنيف
الوظيفي» — قائمة بالقيم الخمس و«—»، تكتب `sub_type` عبر `PATCH
/api/accounting/accounts/<id>/`. هي مخرج صاحب الشركة حين يصنّف الاشتقاق حساباً
خطأً، فلا يبقى الحقل رهينة تخمينٍ لا يُردّ.

## قيد اليومية — وضعان: بسيط افتراضاً ومتقدّم عند الحاجة

`AccountingJournalEntryPage.tsx` يفتح على **الوضع البسيط**: سطرٌ واحد فيه
البيان والحساب المدين والحساب الدائن والمبلغ. **المبلغ يُكتب مرّة واحدة**
ويملأ الجهتين، فلا فرقَ يُطارَد ولا «تعبئة الباقي». والبيان **ملاحظةٌ واحدة
للقيد كلّه** ترتفع بارزةً فوق السطر (ولا تتكرّر في الترويسة)، ويبقى توليدها
التلقائي «من ح/ … إلى ح/ …» كما كان.

زرّ **«متقدم»** يكشف الشبكة الكاملة كما كانت: سطورٌ بلا حدّ، مدين/دائن لكل
سطر، جهة ومركز تكلفة وبيانٌ لكل سطر. والقيد المركّب (ثلاثة أطراف فأكثر)
**يُفتح متقدّماً حتماً** ولا يقبل الطيّ إلى البسيط — الزرّ يُعطَّل ولا يُخفي
سطراً. الطيّ ممنوع أيضاً على أي سطرٍ محفوظ بلا مبلغ، كي لا يُسقَط بصمت.

الحالة واحدة (`lines`) في الوضعين — الوضع البسيط يكتب على السطرين الأوّلين
لا غير، فالتحقّق والتوازن وحمولة الحفظ مسارٌ واحد لا مساران. حساب الذمم
العامّ (`sub_type` = `receivable`/`payable` وغير مربوط بطرف) يُظهر حقل
«الجهة» في الوضع البسيط أيضاً: رقمٌ على الذمم بلا طرفٍ رصيدٌ لا صاحب له.

## تحصيل فاتورة المبيعات — سطحٌ واحد داخل المحرّر

`SalesInvoiceEditor.tsx` (`collectPanel`) يحمل **لوحة التحصيل** الوحيدة في جانب
المبيعات: «المدفوع نقداً» (بمبلغه وصندوقه) · «المدفوع شيكات» (بمجموعه وصفوفه:
رقم/بنك/استحقاق/مبلغ) · «من رصيد العميل» (تظهر حين يوجد سندٌ مرحّل غير موزَّع) ·
و«المتبقي» **مشتقٌّ للقراءة فقط** يتحرّك مع كل حرف.

- **نداء واحد**: `collectSalesInvoice` (`services/salesApi.ts`) ⇒
  `POST /api/sales/invoices/{id}/collect/`. على المسودة تُحفظ الفاتورة ثم يُمرَّر
  `post_invoice: true` فتُرحَّل وتُحصَّل في معاملة الخادم نفسها — لا ترحيلٌ منفصل
  ولا نافذة وسيطة. وردّ النقطة هو الفاتورة بعد التحصيل، تُطبَّق كما هي بلا جلبٍ ثانٍ.
- **الفائض مسموح ومُعلَن**: تجاوز المدفوعِ المتبقّيَ يُظهر «الفائض N يُسجَّل دفعة
  على الحساب» — والخادم هو من يُرحّله دفعةً مقدَّمة؛ الواجهة لا تقسّم الطلب ولا
  تطرح مسبقاً.
- **الفاتورة النقدية**: تحصيلٌ لا يغطّي إجماليها يرفضه الخادمُ ويرتدّ كلُّ شيء،
  فاللوحة تمنع الإرسال أصلاً وتعرض مخرجَين — «أكمل المبلغ نقداً» أو رفع علامة
  «مدفوعة» لتصير على الذمم. رفضٌ كان بالإمكان منعه شاشةٌ سيّئة لا حارس.
- **اللوحة خارج حاوية الإدخال** في شجرة `KitDocumentShell`: الفاتورة المرحّلة
  تُفتح في وضع العرض حيث تلك الحاوية مخفيّة، والتحصيل عليها من المكان نفسه. وهي
  تظهر في الوضعين السهل والمتقدّم (القناع عرضٌ لا منع).
- **الدفع مسارٌ واحد على الجانبين (T-INTENT)**: `SettleFromOnAccountModal`
  حُذف، ومعه نموذج «وصل دفع للمورد» الذي كان داخل `PurchaseInvoiceAccountingPanel`
  — صار الدفع كلّه من `DocumentPaymentPanel` داخل المحرّر (نقد + شيكات + رصيد
  الطرف في نداء واحد). ثلاثة مداخل لعملٍ واحد كانت تعني ثلاث قواعد تفترق.
- **حفظ الدفعة على المسودة** (`onSaveIntent` ⇒ `payment-voucher/` بيعاً و
  `attach-payment/` شراءً): زرٌّ ثانٍ يظهر على المسودة وحدها بجانب «حفظ وترحيل
  وتسجيل دفعة». يسجّل الدفعة بلا أثرٍ في الدفاتر، فيصير «المتبقي» صفراً موسوماً
  «غير مرحّلة» — ما كان مستحيلاً حين كان تسجيلُ الدفعة يوجب الترحيل.
- **`InvoicePaymentsSection`** (`components/shared/`): جدول دفعات المستند داخله،
  في وضعَي التحرير والعرض على الجانبين — سنداتٌ مرحّلة تُفتح وتُطبع، وصفوفُ
  نيّةٍ تُعدَّل وتُحذف. حلّ محلّ جدولٍ كان يظهر في وضع العرض وحده وزرٍّ مكتوبٍ
  عليه «طباعة السند» يفتح قائمة السندات.
- **«المتبقي» من مشتقّة واحدة**: `deriveInvoiceSettlement`
  (`components/shared/DocumentPaymentPanel.tsx`) تخدم الشريط وعرض المستند
  والطباعة والقائمتين — كانت أربع حسبات في محرّر البيع وحده، إحداها بلا
  `Math.max` فتُظهر متبقّياً سالباً على مسودة.
- **«مدفوعة» — دفعةٌ غير مرحّلة على المسودة، تعبئةٌ على المرحّلة (T-PAYFULL3)**:
  التسديد التام هو الحالة الغالبة، وكتابةُ المبلغ يدوياً كانت ضريبةً عليها.
  الزرّ في شريط المحرّرين (`fillCollectFull` بيعاً · `fillPayFull` شراءً)
  **على المسودة يسجّل دفعةً غير مرحّلة** بكامل المتبقّي — نيّةٌ تُحفظ عبر
  `writeIntent` وتظهر موسومةً في جدول الدفعات، وتتجسّد سنداً واحداً عند
  الترحيل. لا قيدَ يُكتب الآن: مالٌ سجّله المستخدم لا مالٌ تحرّك في الدفاتر.
  **وعلى المرحّلة لا وجود لهذه الحالة أصلاً** (سندٌ أو لا شيء) فيبقى الزرّ
  تعبئةً لخانة النقد والتنفيذ بـ«تسجيل دفعة» — ومعه زرّ «المتبقي كاملاً» في
  اللوحة (`onFillFull`). والأساس على المسودة **`remainingAfterIntent`** لا
  `remaining`: النيّة المحفوظة تتجسّد سنداً عند الترحيل، فالاحتساب بالخام
  يسجّل الدفعة مرّتين — وضغطتان على «مدفوعة» لا تُضاعفانها.
  و**المبلغ يُشتقّ من رقم الخادم بعد الحفظ لا من رقم الشاشة** (T-PAYFULL4):
  نقطتا تعليق النيّة ترفضان ما يتجاوز إجمالي الفاتورة **المخزَّن** — البيع
  يعيد حسابه بـ`recalculate_invoice_amounts` والشراء بـ`payable_total` — فكان
  يكفي فارقُ قرشٍ بين حسبة الشاشة وحسبة الخادم (خصمُ سطر، تقريبُ ضريبة، سطرٌ
  لم يُحفظ) ليردّ الطلب 400 ويبدو الزرّ كأنه لم يعمل. الزرّ الآن يحفظ، ثم يبني
  على ما أعاده الخادم (`handleSaveDraft().detail` · `lastSavedRef`)، ثم يعلّق
  النيّة — والكتابة بدلالة الاستبدال فالضغطة الثانية تُنتج الرقم نفسه.
  وبعد النجاح يُمرَّر إلى **جدول دفعات المستند** حيث يظهر الصفّ فعلاً، وعند
  الرفض يُمرَّر شريطُ الخطأ إلى المرأى: الزرّ في الشريط أعلى الشاشة والسبب
  يسكن رأس المستند، فمن ضغط وهو منزلقٌ إلى أسفل الفاتورة كان يرى «لم يحدث شيء».
- **الكبسة نفسها من القائمتين**: زرّ «مدفوعة» على صفّ الفاتورة غير المسدَّدة
  (`InvoiceList.tsx` · `SalesInvoicesPage.tsx`) يفتحها بـ`?pay=full`، فيقرأه
  المحرّر (`autoFillPayFull` · `autoFillCollectFull`) ويعبّئ اللوحة مرّةً
  واحدة بحارس `ref` **بعد** أن يصل الصندوق الافتراضي — لوحةٌ معبّأة بلا صندوق
  تُغري بضغطةٍ يرفضها الحارس.
- **و«حفظ وترحيل» يسجّل ما في اللوحة لا يبتلعه (T-PAYFULL2)**: الزرّ الأساسي
  كان يحفظ ويرحّل ويمضي، فيضغط المستخدم «مدفوعة» ثمّ إيّاه فتُرحَّل الفاتورة
  **غير مدفوعة** ويبقى الرقم معلّقاً في شاشةٍ تبدو كأنها نفّذته — زرّان
  متجاوران أحدهما يبتلع عمل الآخر. `handleSaveAndPost` على الجانبين يتحوّل
  إلى `submitPayment({ saveFirst: true })` / `submitCollect` متى كانت اللوحة
  قابلةً للإرسال (`payment.canSubmit`): نداءٌ واحد ذرّي (`pay/` · `collect/`
  بـ`post_invoice`) يحفظ ويرحّل ويُخرج السند — أو لا شيء. و«مدفوعة» صارت
  تُعلن ما فعلته وتقول أيُّ زرٍّ ينفّذه؛ وعلى فاتورةٍ بلا بنود تقول «لا مبلغ
  بعد» لا «مسدَّدة بالكامل».
- **سؤال «الاستلام مع الترحيل» يلزم مسار الدفع أيضاً**: `pay/` يرحّل داخله،
  فكان الترحيلُ عبره يترك الإعداد العام يقرّر مصير المخزن وحده — أي أثرٌ
  مخزنيّ يختلف باختلاف الزرّ الذي أطلق الترحيل. `askReceiveChoice`
  (`InvoiceForm.tsx`) مصدرٌ واحد للمسارين، والجواب يسافر في جسم `pay/`
  (`receive_on_post`) إلى `post_to_accounting` نفسها.
- يحرسها `e2e/sales-invoice-collect-panel.spec.ts` و
  `e2e/purchase-invoice-save-and-pay.spec.ts` بـAPI مموَّه — بما فيه أن
  **المستدعي** يعرض اللوحة فعلاً (`tsc` لا يفحص خصائص JSX هنا).

## البحث المدمج — قاعدة واحدة تُختبر وحدها

`utils/autocompleteRank.ts` (`rankOptions`) قاعدةُ ترتيب نتائج `KitAutocomplete`
كلِّها. مقعدها في `utils/` لا في المكوّن لأن ملف JSX لا يمرّ من `node --test`.

- **`keywords`** حقلٌ اختياري على كل خيار: نصٌّ يُبحَث فيه ولا يُعرض — SKU
  والباركود وأرقام كتالوج الموردين والهاتف. قبله كان البحث في الاسم والسطر
  الثانوي وحدهما، فرقمُ المنتج — وهو ما يكتبه البائع فعلاً — لا يجد شيئاً وإن كان
  محمولاً في البيانات.
- **كلّ كلمة تُطابَق وحدها**: «اطار 17» يجد «اطار ميشلان 17». كانت المطابقة على
  النصّ كاملاً فتفشل دائماً مع كلمتين.
- **`onShowMore`**: صفّ «عرض N نتيجة أخرى» حين يُخفي السقفُ مطابقات، ينقل
  الاستعلام إلى الفهرس الكامل (`initialSearch`) بدل أن يصمت عمّا لم يعرضه.
- **فشل الباركود يقول لماذا**: مسحةٌ بلا تطابق تامّ كانت لا تفعل شيئاً في محرّر
  البيع؛ صارت تفتح الفهرس على النصّ نفسه مع رسالة.
- يحرسها `utils/autocompleteRank.test.ts`.

## رفع الصور — منطقة واحدة ظاهرة، ولصقةٌ لا يفوز بها إلا واحد

كل إرفاق صورة في التطبيق يمرّ من `frontend_v2/components/ui/FileDropZone.tsx`
(`FileDropZone`): **اختيار بالضغط + سحب وإفلات + لصق من الحافظة (Ctrl+V)** في سطحٍ
واحد يحمل نصّ التلميح ويفحص النوع والحجم ويردّ برسالة `useToast` عربية — لا `alert`
ولا رفضٌ صامت.

- **المنطقة لا ترفع شيئاً.** مخرجها الوحيد `onFiles(files)`، فيبقى قرار الرفع عند
  الشاشة: الفوري ينادي `cloudinaryService.uploadFile` ويرفع `busy`، والمؤجَّل يدفع
  `File[]` في حالته حتى الحفظ. لولا ذلك لارتبط المكوّن بـcloudinary وبنطاق الرفع.
- الحدّ الافتراضي 25 م.ب = **سقف الخادم الحقيقي** (`core/media_views.py` —
  `MAX_UPLOAD_BYTES`)، فالفحص هنا تجربةُ استخدام لا حماية؛ الحماية خادمية كما هي.
- `variant='compact'` للخانات والصفوف الضيّقة، و`capture` يمرّ كما هو لكاميرا الهاتف،
  و`previewUrls` + `onRemove` تعرضان شبكة المعاينة مع الحذف لمن لا معاينة خاصة له.
- **`ariaLabel` حين يُقصّ النصّ المرئي**: الاسمُ المتاح للحقل كان يتبع `hint`، فمربّعٌ
  ضيّق يختصر عبارته (`hint="إدراج صورة"`) يفقد وصفَه لقارئ الشاشة معه. الوصف الآن
  لا يتبع الحيّز — `ariaLabel` يُكتب على `<input>` مستقلاً عن التلميح.

**توجيه اللصق** يسكن `frontend_v2/utils/clipboardImage.ts`: سجلٌّ لمناطق اللصق
بمستمع `paste` **واحد** على المستند مهما بلغ عددها، وكل لصقة تذهب إلى **منطقة واحدة
على الأكثر** يحسمها `resolvePasteZone` — دالة صرفة مُختبَرة في
`frontend_v2/utils/clipboardImage.test.ts`. الترتيب:

1. هدف اللصق داخل عنصر منطقة مفعّلة ← تلك المنطقة (الأعمق أولاً).
2. الهدف حقل نصّي (`input`/`textarea`/`contenteditable`) ← **لا منطقة**؛ لصق النصّ
   لا يُخطَف أبداً.
3. وإلا ← صاحبة آخر نشاط (نقر/تركيز/سحب داخلها، أو التركيب لمن لا تشترط تفاعلاً) —
   فالنافذة المفتوحة فوق الصفحة تكسب لصقتها، والشاشة ذات المنطقة الواحدة تبقى
   «Ctrl+V في أي مكان» كما تعوّد المستخدم.
4. لا منطقة مؤهّلة ← لا شيء.

- `interactionRequired` (صفوف متكرّرة كأقساط الفاتورة): لا تفوز أبداً بترتيب التركيب،
  بل بالاحتواء أو بعد نقر/سحب صريح داخلها — فلا يخطف صفٌّ لصقة جاره.
- `usePasteImageUpload(onFiles, enabled)` بقيت **غلافاً رفيعاً** فوق `usePasteZone`
  بمنطقة تغطّي الشاشة كلها: الشاشات التي كانت تستعملها لم تتغيّر ودخلت قاعدة الفائز
  الواحد مجاناً — ولم يوجد نمطان في المستودع في أي لحظة.
- **العطل الذي أزاله السجلّ**: قبله كان كل مستهلك يركّب مستمعاً خاصاً به على المستند،
  فمستهلكان مركَّبان معاً يرفعان الصورة الملصوقة **مرتين**.
- توزيع الصور الملصوقة على خانات ثابتة العدد (شاشة المنتج: ثلاث خانات) قاعدةٌ واحدة
  صرفة: `frontend_v2/utils/imageSlots.ts` (`fillFirstEmptySlots`) — أول خانة فارغة،
  ثم التالية بالترتيب، وما زاد يُعاد عدده لتخبر الشاشة المستخدم به.

## المستند مركز سياق — تبويبات فاتورة المبيعات

`SalesInvoiceEditor.tsx` يمرّر إلى شريط تبويبات `KitDocumentShell` سبعةَ
تبويبات، ثلاثةٌ منها سياقية في `components/sales/InvoiceContextTabs.tsx`:
**حركة المخزون** · **حساب العميل** · **المرفقات**. ومعها القائمُ سلفاً: سجل
النشاط (`EntityActivityLog`) والحركات المالية المرتبطة (`DocumentPaymentsTab`).

- **الكسل بالبناء لا بالحيلة**: الغلاف يرسم `tab?.content` للتبويب النشط وحده،
  فعنصر React المُنشأ في المصفوفة لا يُركَّب ولا يجلب حتى يُفتح تبويبه. فتحُ
  الفاتورة لا يُصدر أياً من نداءات التبويبات — يقيسه
  `e2e/sales-invoice-context-tabs.spec.ts` بعدّ النداءات.
- **موضع التبويب في المصفوفة لم يعد يهمّ**: الغلاف صار يتتبّع التبويب النشط
  بمعرّفه (`utils/tabSelection.ts`)، فإدراج تبويب في الوسط وقت التشغيل لا يزيح
  المستخدم. كانت القاعدة سابقاً «ألحِقه آخر القائمة» — وقد سقطت.
- **المرفقات وحدها خارج `showAdvancedTabs`**: إرفاق صورة إيصال فعلٌ أساسي لا
  متقدّم. وهي تُحفظ **فوراً** عبر نقطتها الخاصة لا مع حفظ الفاتورة، فتبقى
  ممكنةً على فاتورة مرحّلة (لا تُعدَّل بـPATCH أصلاً)، وترث اللصق والسحب من
  `FileDropZone` بلا مستمعٍ ثانٍ.
- **«قبل/بعد» يأتي من الخادم**: `anchor` في ردّ `customer-ledger/` — مرساةُ
  `partner_account_statement` نفسها. **لا تحسب الأثر في الواجهة**، وحقلا
  `customer_balance_*_invoice` في حمولة الفاتورة تقريبٌ لا يطابق كشف الحساب
  (أُزيلا من عرض المستند لهذا السبب — التفصيل في `docs/modules/sales.md`).
- **شريط الحالة يحمل «رقم الحركة»** بجانب رقم القيد — سلوكٌ موروث من الشاشة
  الكلاسيكية (سببه مؤرشَف في `docs/aseel_reference/invoices.txt`: «رقم الحركة
  المخزنية المسلسل… يمكن استخدام هذا الحقل للاستعلام عن الحركات»؛ أرشيفٌ لا
  مرجع تصميم). يصل ضمن حمولة الفاتورة
  (`stock_movement_no`) فلا يكلّف نداءً.

## «هل تعلم» — تلميحات سياقية

بطاقةٌ واحدة تشرح ميزةً قائمة قليلة الاستعمال، تسكن الفراغ أسفل الشاشة الحالية.
ثلاثة ملفات لا رابع لها:

- `frontend_v2/constants/tips.ts` — **كل النصوص العربية هنا وحدها**، عشرة تلميحات،
  لكلٍّ `key` و`view` (شاشة الظهور) وعنوان وسطران و`targetView` اختيارية. نوعا
  `view`/`targetView` هما `AppView`، فمفتاح شاشةٍ مخترَع يكسر البناء بدل أن يمرّ
  تلميحاً يتيماً لا يظهر أبداً.
- `frontend_v2/utils/didYouKnowPref.ts` — كل القواعد، دوالّ صرفة تُختبر بلا متصفح:
  الاختيار (`pickTip`)، والتفضيل لكل مستخدم في `localStorage` بمفتاح
  `ktra.didYouKnow:<userId>`، وقاعدة المساحة (`hasRoomForTip`).
- `frontend_v2/components/ui/DidYouKnow.tsx` — العرض وحده، مركَّبٌ **مرّة واحدة**
  في `App.tsx` داخل `<main>`، بلا حرفٍ عربيّ من نصوص التلميحات.

القواعد:

- التلميح يظهر في **شاشته وحدها** — تلميح الشيكات في شاشة الشيكات لا في الرئيسية.
- **يُعرض مرّتين ثم يُطوى وحده**، وزرّ «لا تُظهر هذا مجدداً» يُنهيه فوراً — لكل
  تلميح على حدة ولكل مستخدم على حدة. وعند التساوي يفوز الأقلّ ظهوراً فتتناوب
  التلميحات بدل أن يستهلك واحدٌ رصيده ثم يبدأ الذي يليه.
- **المساحة تُقاس فعلياً** من حارسٍ صفريّ الارتفاع في نهاية التدفّق، وبعد أن يهدأ
  ارتفاع المحتوى (قياسٌ فوريّ كان يُظهر البطاقة لحظةً ثم يبتلعها حين يصل الجدول).
  بلا متّسع لا تُعرض أصلاً: لا تزيح عنصراً ولا تطفو فوقه.
- **الأهلية مشتقّة** من `utils/viewPermissions.ts` و`utils/uiMode.ts` لا من حقل
  صلاحية يُكتب في التلميح — نسخةٌ ثانية من خريطة الصلاحيات كانت ستنحرف بصمت. وهي
  تشمل شاشة التلميح **نفسها** لا وجهته وحدها: `App.tsx` يسقط على لوحة التحكم حين
  تُحجب الشاشة بينما تبقى `appView` على الشاشة المحجوبة.

التفضيل محليّ لا خادميّ عن قصد: عدّاد الظهور محلّيٌ بطبعه (كتابة كل ظهورٍ للخادم
ثرثرةٌ بلا مقابل)، وثمنُه المدفوع صراحةً أن «لا تُظهر مجدداً» لا يتبع الحساب عبر
الأجهزة — و`didYouKnowPref.ts` هو المقعد الوحيد للتبديل لاحقاً إلى حقلٍ على
العضوية (على غرار `ui_mode`) بلا مسّ المكوّن. برهانها
`frontend_v2/utils/didYouKnowPref.test.ts` و`frontend_v2/e2e/did-you-know.spec.ts`.

## التبويبات المتصفّحية — «من أين جئتُ» و«إلى أين أرجع»

الأصيل تطبيق MDI يعرف نوافذه؛ المتصفّح لا يعطي التطبيق هذه القائمة. النظام يفتح
مستنداته في تبويبات جديدة عبر **مصدر واحد** — `frontend_v2/utils/openInNewTab.ts`
(`openInNewTab`) — فبُنيت فوقه ثلاث قطع:

- **`utils/tabLink.ts`** — مناولةٌ وحضور. عند الفتح يُكتب سجلّ محلي بمُعرّف
  التبويب الفاتح واسم شاشته، ويُلحق رمزٌ عابر (`_ktab`) **بالمسار الداخلي وحده**،
  **في المرساة (`#`) لا في الاستعلام (`?`)**. التبويب الجديد يلتقطه في
  `captureTabHandoffOnBoot()` **قبل إقلاع React** (`index.tsx`، بجانب
  `applySkinOnBoot`)، يستهلك السجلّ مرّةً واحدة، وينظّف الرابط بـ`replaceState` —
  فلا يرى الموجّه الرمزَ العابر ولا يتسرّب إلى رابطٍ يُنسخ. `sessionStorage`
  و`window.name` كلاهما مرفوض هنا: يسقط مع `rel="noopener"` الذي نفتح به.

  **ولماذا المرساة؟** مفتاح `Cache API` يشمل الاستعلام ويتجاهل المرساة، و`sw.ts`
  يخزّن كل تنقّل ناجح (`networkFirst`). فرمزٌ استعلاميّ — وهو UUID جديد لكل فتحة —
  كان يترك نسخةً دائمة من `index.html` في كاش المتصفّح **لكل تبويب يُفتح**، بلا
  سقف حتى النشرة التالية (`purgeCachedApiResponses` لا يمسّ إلا `/api/`). مقيسٌ في
  المتصفّح: ثلاث فتحات ⇒ 3 مدخلات بالاستعلام مقابل مدخل واحد بالمرساة. والمرساة
  لا تُرسَل للخادم أصلاً فلا تلوّث سجلّات الوصول ولا كاش أي وسيط. **لا يظهر هذا في
  التطوير إطلاقاً**: الـservice worker مُطفأ هناك (`devOptions: { enabled: false }`)
  — فأي معامل عابر جديد على مسارٍ يُتنقَّل إليه يجب أن يُقاس على بناء الإنتاج. والحضور عبر `BroadcastChannel` باسم `ktra-tabs`،
  **بلا نبضٍ دوري** (مؤقّتٌ يعيد الرسم كل بضع ثوانٍ عطلٌ معروف في هذا المستودع):
  إعلانٌ عند الإقلاع وعند تغيّر الشاشة، ووداعٌ عند `pagehide`.
- **`components/layout/TabAwareness.tsx`** — `TabOriginChip` يومض وميضةً واحدة
  (نصف ثانية، `.ktra-flash-once`) ثم يهدأ إلى شارةٍ تقول «تبويب جديد · من
  «كذا»»؛ و`LinkedTabHint` لمحةٌ **واحدة لكل تبويب** عند ضغط «رجوع» تذكّر بالتبويب
  الآخر. لا زرّ «انتقل إلى ذلك التبويب» عمداً: المتصفّحات تمنع `window.focus()`
  من تبويبٍ غير مُفعَّل و`noopener` يقطع المقبض، وزرٌّ لا يعمل أسوأ من غيابه.
- **`utils/backTarget.ts` + `hooks/useAppBack.ts`** — زرّ «رجوع». كان
  `navigate(-1)` عمياء، وفي تبويبٍ فُتح على مستند مباشرةً لا سابقة فيه: الضغطة
  بلا أثر أو تقذف المستخدم خارج التطبيق. الآن `history.state.idx` (يكتبه
  `react-router`) يحسم: `> 0` رجوعٌ حقيقي، وإلاّ انتقالٌ إلى **قائمة الشاشة
  نفسها** (`VIEW_PATHS[activeView]` يمرّره `App.tsx` إلى `AppLayout` ثم
  `Breadcrumb`) أو الرئيسية — واسم الوجهة يُكتب على الزرّ. الاسم المتاح يبدأ
  دائماً بـ«رجوع» ليبقى للزرّ هوية ثابتة (وشرط WCAG 2.5.3).

قواعد هذه المنطقة:

- **`prefers-reduced-motion` يُسقط الحركة ويُبقي الشارة** — المعلومة ليست حركة،
  وإخفاؤها مع الحركة يحرم مَن يحتاجها أكثر.
- **المؤشّر مرّةً لا متكرّراً**: المناولة تُستهلَك عند أول قراءة، فتحديث الصفحة لا
  يعيد المؤشّر؛ ولمحة «لديك أيضاً» علَمٌ على مستوى الوحدة لا حالة مكوّن.
- **إعلانان لا واحد**: التبويب الجديد يعلن عن نفسه، **والفاتح يعلن أيضاً**
  (`toast`) — المتصفّح قد يفتح التبويب في الخلفية فلا يرى المستخدم شيئاً ويظنّ
  الضغطة لم تعمل.
- **كل فتحٍ لمسار داخلي يمرّ بـ`openInNewTab`** لا بـ`window.open` مباشرةً، وإلاّ
  وصل التبويب بلا هويّة. (نوافذ الطباعة والصور `window.open("")` تبقى كما هي —
  ليست شاشات تطبيق.)
- سجلّات المناولة التي لم تُلتقط (تبويبٌ حجبه مانع النوافذ) تُكنَس عند كل إقلاع
  (`sweepStaleHandoffs`) فلا تتراكم في `localStorage`.

البراهين: `utils/tabSelection.test.ts` · `utils/tabLink.test.ts` ·
`utils/backTarget.test.ts` · `e2e/new-tab-indicator.spec.ts` (تبويبان حقيقيان) ·
`e2e/back-button.spec.ts`.

## T-WIN — النوافذ العائمة وشريط الإجراءات

**النافذة العائمة للمحتوى الثانوي وحده.** المستند الرئيسي يبقى ملء الشاشة كما في
Odoo/Zoho — لا MDI حرّ: هو يخالف نمط الويب ويهدم قرار «المُمرِّر الواحد»
(`main.app-content`). العائم هو ما يُقارَن بالمستند أو يُستدعى أثناءه: بطاقة
المنتج، معاينة الملف، الحاسبة.

`components/kit/KitFloatWindow.tsx` — نافذةٌ واحدة تُسحب من شريط عنوانها
وتُحجَّم من حوافها الأربع وزواياها الأربع. `portal` إلى `body`، و`role="dialog"`،
وESC، وحبس Tab وإعادة التركيز لمن فتحها، وشريط سفلي مثبَّت (`footer`) لأزرار
القرار فلا تنزلق مع المحتوى. تحت 768px تصير لوحاً ملء الشاشة بلا سحب ولا تحجيم
والهندسة المحفوظة تُتجاهل.

الحساب كلّه في `utils/windowGeometry.ts` نقيّاً بلا DOM (مُختبَر في
`windowGeometry.test.ts`) والحلقة المؤشِّرة وحدها في `hooks/useDragResize.ts`:
Pointer Events + `setPointerCapture` — حدثٌ واحد للفأرة واللمس والقلم، ولا يسقط
السحب السريع كما يسقط في نمط `mousemove` على المستند.

**الموضع بـ`left/top` بكسلاً خاماً فلا قلب إشارة في RTL** — بخلاف عرض الأعمدة في
`columnWidths.ts` حيث الحافة نفسها تنعكس.

**الهندسة تُحفظ في `ktra:win:<الاسم>`، وتُقصّ عند الاسترجاع** — شاشةٌ تغيّر
مقاسها بين جلستين تُعيد نافذةً محفوظةً خارج المرأى بلا هذا القصّ.

**شريط الإجراءات**: `layout/ActionBarRail.tsx` يلفّ `GlobalActionBar` بجهةِ
إرساءٍ محفوظة في `ktra:actionBarDock` (`right` افتراضاً · `left` · `top`).
`top` يعرض حرفياً الترميز الأفقي التاريخي في شريط العنوان — فهو مفتاح التراجع
وهو المفروض على الجوال (بلا كتابة تخزين، فتفضيل الحاسوب يبقى). الرفّ يُحقن
بـ`createPortal` لأن شريط العنوان سياق تراص (قاعدة 4.1).

**سلّم z الجديد مضاف لا مُعاد ترقيمه**: `--z-float-rail: 60` · `--z-window-mask: 80`
· `--z-window: 81` — فوق لوح المنتقي (70) وتحت طبقة المنبثقات (10000+) عمداً:
قائمة إكمالٍ تُفتح داخل نافذة يجب أن تعلوها.

البراهين: `utils/windowGeometry.test.ts` · `e2e/kit-float-window.spec.ts` ·
`e2e/action-bar-dock.spec.ts`.

**شريط الوصول السريع يُطوى — وشريطُ الإجراءات لا يُخلط به**: `layout/QuickAccessBar.tsx`
هو الشريط الأفقي أعلى المحتوى (رجوع + مسار الشاشة + بطاقات الاختصارات + مرسى
«مرشد الرحلة»)، وهو شريط **وصول** لا **إجراء** — `ActionBarRail` أعلاه شيء آخر.
يُطوى بزرّه أو بـ`Ctrl+F1` (وتر طيّ شريط أوامر Office)، والتفضيل يُحفظ **لكل
مستخدم** في `ktra.quickBar.open:<id>` (`utils/quickBarPref.ts`) — جهاز المحلّ
مشترك، وطيّ صاحبه لا يسلب موظّفه زرّ «رجوع». ثلاث قواعد لا تُكسر:

1. **الطيّ ارتفاعٌ صفر لا حذفٌ من الشجرة** (`grid-template-rows: 0fr`):
   `ImportJourneyGuide` يمسك عقدة `#import-guide-slot` مرّةً بـ`getElementById`
   ويصبّ فيها `createPortal` — حذفُ العقدة يترك البوابة تصبّ في عقدةٍ منفصلة
   عن الصفحة، فيختفي زرّ «مرشد الرحلة» ولا يعود بالبسط.
2. **`inert` مع `aria-hidden` عند الطيّ** — بلاه يبقى المخفيّ في مسار `Tab`.
3. **اللسان يبقى مرئياً دائماً مطويّاً** — لا إخفاء تلقائي ولا حالة بلا مخرج.

ومعها حارسٌ في `components/kit/useKitKeymap.ts`: المفاتيح الوظيفية هناك
**مجرّدة**، فأيّ وترٍ مع `Ctrl`/`Meta` لا يُفهم مفتاحاً مفرداً (كان `Ctrl+F1`
يفتح بحث المستند معه، و`Ctrl+F5` يُطلق إجراء `F5` مع إعادة تحميل المتصفّح).

البراهين: `utils/quickBarPref.test.ts` · `e2e/quick-bar-collapse.spec.ts`.

**الشاشات الشاذة عادت إلى الغلاف**: `frontend_v2/components/inventory/StockLevelsPage.tsx` و
`frontend_v2/components/inventory/StockMovementsPage.tsx` كانتا `div` بأنماط inline (35 موضعاً) خارج
`KitDocumentShell`، وشاشات البيانات الخام الأربع كانت بطاقةً مرتجلة في
`frontend_v2/components/sql/SqlDataPageShell.tsx`. الثلاثة صارت على الغلاف الموحّد بنصوصٍ لم تتغيّر
حرفاً — والأربع ورثت ذلك بتعديل ملفِ إطارهنّ وحده.

**دَينٌ معلوم**: 141 نمطاً مضمّناً باقياً في `frontend_v2/components/sql/` (‏`SqlDealsPage.tsx` وحدها 86) — مخالفةٌ لقاعدة «Tailwind فقط» سابقة لهذا العمل،
تُنظَّف في مهمة مستقلّة لأن تحويلها الآلي بلا تحقّقٍ بصري يخاطر بكسرٍ صامت.

## قواعد لا يجوز كسرها
- **طريقُ الخروج من دفتر العميل لا يعتمد على مفتاح جلسةٍ وحده** (بلاغ المالك): `insideManagedBook` (`contexts/CompanyContext.tsx`) شرطُها الثاني «للعودة وجهةٌ معروفة» = `managedBookOffice()` **أو** `officeTenantId`. كانت تقرأ مفتاح الجلسة وحده، فمن فتح الدفتر بالرابط أو فقد مفتاح تبويبه بقي داخله بلا زرِّ خروج وطريقُه الوحيد مبدّلُ الشركات فوق — **وهو لا يعرض الدفتر أصلاً** (الدفاتر مستثناة من `my-companies`). و`officeTenantId` وجهةٌ صحيحة تماماً: `loadCompanies` تبحث عن المكتب **المالك** للشركة النشطة في كل مكاتب المستخدم لا تكتفي بالأول. والزرّ الآن في موضعين: شريط `ManagedBookBanner` (`layout/AppLayout.tsx`) وأعلى قائمة `CompanySwitcher` — حيث يبحث عنه المستخدم فعلاً.

1. **حارس الشاشة في `App.tsx` صلاحيةٌ لا دور** (ISSUE #62): `canView(appView)`
   لا `currentUser!.role !== "manager"` — نفس المفتاح الذي يحكم الرابط في
   `Sidebar.tsx` (`utils/viewPermissions.ts` — `VIEW_PERMISSIONS`). قناتان
   مختلفتان تعنيان رابطاً يُرى ولا يُفتح، وهو ما كان يقع لدور «محاسب» في كل شاشة
   محاسبة. **وقبل الاستبدال تأكّد من وجود مفتاحٍ للشاشة**: `canView` تعيد `true`
   لشاشةٍ بلا مفتاح، فاستبدالٌ أعمى يفتحها للجميع بدل أن يضبطها. الشاشة التي
   رابطها نفسه محكومٌ بالدور مباشرةً (`client-books`، شاشات `sql-*`) تبقى على
   حارس الدور — لا قناتين هناك أصلاً.
2. **`tsc` لا يفحص خصائص JSX** — لا `@types/react` في المشروع. مكوّن يستقبل prop
   لا يمرّره أحد يبقى ميتاً بلا شكوى. **تحقّق من المستدعي بالقراءة** بعد أي تغيير
   على واجهة مكوّن.
3. **كل رقم عبر `utils/formatNumber`** — لا `toFixed(2)` ولا `toLocaleString`
   للمبالغ (مصدر أصفار زائدة متكرّر).
4. **التواريخ عبر `utils/formatDate`** — `toLocaleDateString` بلغة عربية يعطي
   هجرياً/أرقاماً هندية حسب إعداد الجهاز.
5. **Tailwind فقط — لا inline styles.** وانتبه: قواعد `tbody td` خارج الطبقات في
   `index.css` تُبطل أصناف Tailwind على خلايا الجداول. **ولا تخترع رمز لون**:
   `bg-[var(--اسم-غير-معرَّف)]` يمرّ في البناء وفي كل اختبار ويسقط إلى **شفّاف**
   عند العرض. وقع فعلاً في `ScanLookupPanel` (`--color-bg` لا وجود له؛ المعرَّف
   `--color-surface`) فكانت اللوحة زجاجاً — أمسكه تصويرُ العنصر وحده. القائمة
   المعرَّفة في `styles/index.css`.
5.1. **لوحةٌ تُولَد من الترويسة تحتاج `createPortal`** — الترويسة عنصرٌ
   `sticky` بـ`z-index`، أي **سياق تكديس**، فطبقةٌ `fixed` بداخلها تبقى محبوسةً
   فيه مهما رُفع رقمها. `ScanLookupPanel` يُحقن في `document.body`.
6. **لا تتبُّعَ لاختيارٍ بالفهرس — بالمعرّف دائماً.** كان `KitDocumentShell`
   و`KitTabs` يحفظان التبويب النشط رقماً، فإدراج تبويب في الوسط وقت التشغيل
   يزيح كل ما بعده ويقفز بالمستخدم إلى شاشة أخرى. القاعدة كلّها الآن في
   `frontend_v2/utils/tabSelection.ts` (`resolveActiveTabKey`): اختيار المستخدم
   أقوى، ثم الطلب الخارجي (`initialTab` / `?tab=`)، ثم أول تبويب — وحذفُ التبويب
   النشط يسقط إلى الأول لا إلى لوحة فارغة. وبها صار `initialTab` يُطبَّق **متى
   ظهر تبويبه** لا في أول رسمة وحدها، فالرابط العميق لم يعد يضيع على شاشةٍ تبني
   تبويباتها بعد الجلب. نفس عائلة «حقن أعمدة الشبكة بالمفتاح لا بالفهرس».
6.1. **قِس السطح المتحرّك بمقاييس التخطيط لا بالصندوق المرئي.** النافذة العائمة
   تدخل بـ`scale(.97)`، و`boundingBox()` يقرأ التحويل فيعطي عرضاً متذبذباً؛
   `offsetWidth/offsetLeft` لا يراه. أوقع هذا اختبار السحب مرّتين قبل أن يُصحَّح.
6.2. **حارس التكافؤ يلزمه عضويةُ شركة في مُحاكيه.** `feature-parity-census`
   يُحاكي كل نداء API بـ`[]`؛ وحين دخلت شاشة «لنُنشئ شركتك الأولى» (7923721،
   2026-07-22) بعد تسجيل خط الأساس (70f10c7، 2026-07-21) بيوم، صار التطبيق يقف
   عليها فلا يُرسم `AppLayout` إطلاقاً: ينتظر كل هدفٍ 15ث ثم يُسجَّل skipped،
   وتنتهي مهلة الاختبار (78 × 15ث) **قبل** بلوغ المقارنة. تعطّل الحارس شهراً
   بصمت. المُحاكي الآن يردّ عضويةً واحدة على `/api/tenants/companies/my-companies/`
   وصلاحياتِ مدير على `/api/permissions/me/`، فزمن الجولة 3.7د بدل تجاوز المهلة.
   **القاعدة**: أي بوابةٍ جديدة أمام الغلاف (اشتراك، عضوية، تحقّق) تلزمها
   إضافةٌ مقابلة في مُحاكي الإحصاء، وإلا عمي الحارس بلا أن يحمرّ.
7. **نوافذ الكاش تُفرَغ بعد نجاح الكتابة لا قبلها**، ومع عدّاد أجيال كي لا يعيد
   طلبٌ طائر ملأها بقيمة قديمة (`inventoryApi.ts`, `tenantSettingsApi.ts`).
8. **ما يخصّ المستخدم لا يُدرَج في قوائم الشركة** — مكانه «حسابي»
   (`components/personal/MyAccountPage.tsx`، مسار `/my-account`، مدخله بطاقة
   المستخدم أسفل `Sidebar.tsx`). بندٌ شخصي بين شاشات الشركة يوحي بأنه بيان
   شركة، والحدّ بين المال الشخصي ومال الشركة حدٌّ محاسبي قبل أن يكون حدَّ تنقّل.
9. **النصّ الطويل داخل خلية قائمة يُقصّ، والقصّ يختلف باختلاف ما يُقصّ.** النصّ
   الحرّ (اسم مورد، وصف طلبية) يقع عليه `.ktra-cell-clip` — سطر واحد و«…» في
   الذيل والكامل في `title`. أما **رقم المستند** فما يميّزه تسلسلُه لا بادئتُه:
   في قائمةٍ كلُّ صفوفها `IQ-00xx` تكون البادئة والأصفار ثلثَي الخانة بلا معلومة.
   فالمعروض تسلسلٌ وحده عبر `utils/documentNumberDisplay.ts`
   (`documentSerialDisplay` — `IQ-0006` ← `6`) والكامل في `title` وفي البحث،
   وتُترك المساحة للصورة التي هي المميِّز البصري للصفّ. والحارس على القائمة لا
   على الصفّ: إن جعل الاختصارُ صفّين متطابقين (شاشة الشراء تخلط `PQ` و`PO`)
   تعود القائمة كلّها إلى الرقم الكامل بالقصّ الأوسط (`elideDocumentNumber` —
   من الوسط لأن الذيل يميّز السجلّ والصدر يميّز النوع). وبلا قصٍّ أصلاً يلتفّ
   الرقم سطرين ويزاحم ما بجانبه في الخلية.
10. **كل مسار حفظ له ثلاث حالات مرئية، والصمت ليس حالة.** جارٍ الحفظ (زرّ معطّل)
   · نجح · فشل **مع السبب**. الجديد يمرّ عبر `hooks/useSave.ts`
   (`useSave` — يدير `saving` ويلتقط الخطأ ويستخرج `fieldErrors`)، والقائم
   السليم يبقى كما هو. وعند الفشل **لا تُغلق النافذة ولا تُصفَّر المدخلات ولا
   تُفقد مراجع المرفقات** — المستخدم يصحّح من حيث وقف. سبب الفشل يُعرض في لافتة
   **ثابتة** لا في `toast`: الأخير يختفي بعد ثوانٍ فيترك المستخدم أمام نموذج
   ممتلئ بلا تفسير. وما له حقل مرئي من `err.fieldErrors` يُعرض بجانبه عبر
   `components/ui/FieldError.tsx`، والباقي في اللافتة.

11. **لا `alert` ولا `confirm` ولا `prompt` من المتصفح** — `useToast` ·
   `useConfirm` · `components/common/PromptDialog.tsx`. القاعدة مفروضة آلياً في
   `eslint.config.js` (`npm run lint`) بقاعدتين معاً: `no-restricted-globals`
   للاستدعاء المجرّد، و`no-restricted-properties` لتهجئة `window.*` /
   `globalThis.*` — والأولى وحدها عمياء عن `window.prompt(...)`، وهي التهجئة
   التي أخفت أربعة ملفات مالية عن الجرد اليدوي. بعد أن كانت
   مكتوبة بلا حارس فخُرقت في 36 ملفاً — منها شاشات مالية تأخذ رقم قيد يومية
   وعدد شيكات من `window.prompt`.

12. **لا `<input type="file">` جديد لصورة** — `FileDropZone` وحده، ولا مستمع `paste`
   خاصّ بشاشة: `usePasteZone` أو غلافها. مستمعٌ ثانٍ خارج السجلّ يعني لصقةً برابحين.

13. **الإنشاء حيث انتهى البحث — والمعرَّف يعود إلى الحقل الذي ناداه.** حقلُ طرفٍ
   أو منتجٍ داخل مستند يُكتب فيه (`KitAutocomplete`)، وآخرُ سطرٍ في قائمته
   «إضافة «ما كُتب»» يفتح نافذة الإنشاء **بالاسم معبّأً**؛ ونفسه في
   `KitIndexPicker` عبر `onCreate` حين لا يُرجع البحث شيئاً. وبعد الحفظ لا
   يكفي `setXId(created.id)`: القوائم تصل من الشاشة الأمّ بعد بثّ `eventBus`،
   فيبقى الحقل **فارغاً ومعرَّفه مسنَد** حتى تصل — وعلى شركةٍ فوق سقف
   `lookup` (500) لا تصل أبداً. الصفُّ المُنشأ يُدرَج في حالةٍ محلية تُدمَج مع
   الخاصية (`extraCustomers` / `extraProducts` في `SalesInvoiceEditor.tsx`).
   **ونافذةٌ تُفتح من الحقل مباشرةً يجب أن تُعطَّل معها لوحةُ مفاتيح المستند**
   (`useKitKeymap({ enabled })`) وإلّا صفّر `Esc` داخلها المستندَ كلَّه.

14. **الشريط (ISSUE #83) فرعان في `Sidebar.tsx` — لا مسارٌ ثالث.** `shell`
   (`usePermissions()`) موجودٌ ⇒ يُرسَم من `buildShellSections` (مجموعاته
   وترتيبه وتسمياته من البيان)؛ غائبٌ (`general`، وهو الغالبية) ⇒ الفرع
   القديم **حرفاً بحرف بلا لمسة واحدة**. مجموعات القالب اليدوية
   («المبيعات»/«العملاء»/… حتى «التقارير») لم تُحذف — تعيش الآن داخل فرع
   `else` وحده. الأزرار العامة (المعرض، من نحن، تواصل، إدارة المهام…) خارج
   الفرعين، مشتركة بلا تكرار — **باستثناءين مقصودين لمنع رابطٍ مكرَّر
   بمعنى**: زرّ «الإعدادات» العام يختفي إن كانت «الإعدادات» إحدى مجموعات
   البيان (`manifestCoversSettings`)، وزرّ «وضع المحاسب» (A5) يختفي مع
   البيان (لا مجموعة محاسبةٍ يدوية يعيد ترتيبها). **مفتاحا «الرئيسية»
   و«دفاتر عملائي» كانا مقصورين على المدير بلا مدخلٍ في كتالوج الصلاحيات** —
   `buildShellSections` تُطبّق القاعدة نفسها (`managerOnlyViews`) وإلا
   انفتح البندان لكل الأدوار بمجرّد دخولهما البيان.

## التحقق

```bash
cd frontend_v2
npx tsc --noEmit     # لا يفحص JSX props — انظر القاعدة 1
npm test             # اختبارات utils
npm run lint         # يمنع alert/confirm/prompt المتصفح
npm run build
```

## الاعتماديات

**يعتمد على:** كل الـAPI تحت `/api/` — الفهرس الكامل في `docs/API_INDEX.md`.
