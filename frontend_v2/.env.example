# نسخ إلى .env — عنوان API = Django فقط (ليس OpenClaw)
# VITE_API_URL=http://127.0.0.1:8000/api

# متجر افتراضي لعنوان /store بلا معرّف (اختياري): يحوّل الزائر مباشرة إلى
# /store/<slug> بدل صفحة التعريف. اتركه فارغاً وتظهر صفحة التعريف.
# VITE_DEFAULT_STORE_SLUG=ktra
