/**
 * DEF-009 — مهلة الجلسة عند الخمول.
 * القيمة الوحيدة القابلة للتهيئة لمدة الخمول قبل إنهاء الجلسة. غيّرها هنا فقط.
 * مدّدت إلى 3 ساعات لتغطية عمليات الجرد الطويلة (العدّ من ورقة قد يطول والماوس ساكن).
 */
export const IDLE_TIMEOUT_MS = 3 * 60 * 60 * 1000; // 3 ساعات

/**
 * مهلة التنبيه قبل إنهاء الجلسة: يظهر مودال «ستنتهي الجلسة» بعدّاد تنازلي قبل
 * الانتهاء بهذه المدة، فيستطيع المستخدم الضغط «متابعة» لتمديد الجلسة دون فقد عمله.
 */
export const IDLE_WARNING_MS = 2 * 60 * 1000; // تنبيه قبل دقيقتين
