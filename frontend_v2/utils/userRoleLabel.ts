/**
 * اسمُ دورِ المستخدم كما يُعرَض له — **مصدرٌ واحدٌ لكلّ موضعٍ يطبعه** (212-P1).
 *
 * مفردتان تلتقيان في `types/user.ts` عند `UserRole`:
 *
 * * **أدوارُ العضويّة** في `tenants.models.UserCompanyMembership.ROLE_CHOICES` —
 *   دورُ الشخص في شركةٍ بعينها.
 * * **أدوارُ حمولة المصادقة القديمة** التي يبنيها `hr/auth_api.py`، وفيها
 *   `role = "manager" if user.is_superuser else "employee"`. أي أنّ **كلَّ** من
 *   ليس سوبر أدمن ولا مديرَ عضويّةٍ يصله `employee` حرفيّاً — وهو ما قرأه
 *   المالكُ على شاشته: «الدور: employee».
 *
 * وكانت الخريطةُ تعرف تسعةَ أدوارِ عضويّةٍ ولا تعرف هذين، والموضعان يطبعان
 * المفتاحَ الإنجليزيَّ حين تعجز (`?? user.role`) — فالحالةُ **الغالبة** في
 * النظام كلِّه كانت تُعرَض بالإنجليزيّة.
 *
 * ولذلك قاعدتان هنا:
 *
 * 1. **لا مفتاحَ إنجليزيٌّ يصل الشاشةَ أبداً.** المجهولُ يُعرَض «مستخدم»: اسمٌ
 *    عامٌّ صادقٌ خيرٌ من رمزٍ تقنيٍّ يقرؤه صاحبُ النظام فيبني عليه قراراً — وقد
 *    فعل: قرأ الملصقَ فاستنتج أنّ عزلَ الموظّفين مكسور.
 * 2. **والمجهولُ لا يُترَك مجهولاً**: `core/tests/test_header_role_label.py`
 *    يقرأ مفرداتِ `UserRole` و`ROLE_CHOICES` معاً ويسقط على أوّلِ دورٍ بلا اسم،
 *    فالبديلُ العامُّ شبكةُ أمانٍ للقارئ لا بابٌ خلفيٌّ للصمت.
 */
export const USER_ROLE_LABEL: Record<string, string> = {
  // أدوارُ العضويّة (`UserCompanyMembership.ROLE_CHOICES`).
  manager: 'مدير',
  accountant: 'محاسب',
  legal_accountant: 'محاسب قانوني خارجي',
  sales: 'موظف مبيعات',
  procurement: 'موظف مشتريات',
  staff: 'موظف',
  ess: 'موظف خدمة ذاتية',
  field_staff: 'موظف ميداني',
  viewer: 'مستعرض',
  // أدوارُ حمولة المصادقة القديمة — لا وجودَ لها في العضويّة.
  // و`employee` هي حالةُ «ليس سوبر أدمن ولا مديرَ عضويّة» لا دورٌ يُمنَح.
  employee: 'موظف',
  store_guest: 'زائر متجر',
};

/** اسمُ الدور، وأبداً لا مفتاحُه. */
export const userRoleLabel = (role: string | undefined | null): string =>
  USER_ROLE_LABEL[String(role ?? '')] ?? 'مستخدم';
