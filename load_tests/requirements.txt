# اعتماديات اختبار الحمل — **منفصلة عمداً عن requirements.txt الإنتاجي**.
#
# لا يُثبَّت أيٌّ من هذا على خادم الإنتاج: locust يجرّ gevent/flask/pyzmq
# ومنظومة ويب كاملة خاصة بمولّد الحمل، ووجودها في بيئة الخادم يزيد سطح
# الاعتماديات بلا مقابل. مولّد الحمل يعيش على جهاز/حاوية القياس، لا على الخادم.
#
# التثبيت (بيئة افتراضية مستقلة عن venv المشروع):
#     python -m venv .venv-load && . .venv-load/bin/activate
#     pip install -r load_tests/requirements.txt
#
# ── لماذا 2.46.0 بالذات ──────────────────────────────────────────────────
# سقف السيرفر Python 3.10 (موثَّق في requirements.txt الإنتاجي وفي
# docs/SCALABILITY_AUDIT.md §6 / P2-5). تحقّقنا من بيانات PyPI مباشرةً
# (https://pypi.org/pypi/locust/json) بتاريخ 2026-08-11:
#   • 2.46.0  → requires_python ">=3.10"  ✅ آخر إصدار مستقر يقبل 3.10
#                (رُفع 2026-07-19، غير مسحوب/yanked، له wheel و sdist)
#   • 2.46.1 فما فوق → requires_python ">=3.11"  ❌ لا يعمل على 3.10
# لذلك 2.46.0 هو **أحدث** نسخة مستقرة متوافقة، لا نسخة قديمة اخترناها احتياطاً.
# ملاحظة: مولّد الحمل قد يعمل على مفسّر أحدث من مفسّر الخادم، لكن تثبيت
# النسخة على سقف 3.10 يُبقي العدّة قابلةً للتشغيل من داخل بيئة مطابقة للخادم
# (حاوية/جهاز النشر نفسه) بلا مفاجآت.
locust==2.46.0
